# build output
build/
*/build/
.gradle/
tools/vectorgen/out/

# local machine config (contains the SDK path)
local.properties

# runtime data - NEVER commit: seed.txt is a spendable Minima wallet seed
data/
*/data/
*.log
seed.txt

.DS_Store

# APKs are 60MB since WebRTC - they live in dist/ on disk and on GitHub
# Releases, never in git history (server jars stay tracked - they are small).
dist/*.apk
