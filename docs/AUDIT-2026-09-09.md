# Parlons family audit — 9 September 2026

**Audit pass closed at the owner’s request:** the in-flight gateway/relay repair, validation and affected dependency propagation are complete. New audit branches are out of scope. **Wake-proxy centralisation remains unresolved; decentralisation is not certified or fully satisfied by this pass.** Reliability fixes to the proxy do not resolve that architectural issue. See [the closeout report](AUDIT-CLOSEOUT-2026-09-10.md) for final scope, evidence, delivery status and deferred work.

Decentralisation was a blocking requirement throughout this audit. The changes retain independent relay operators, user-selected bootstrap relays, signed directory records, redundant discovery, local custody, and classic Maxima interoperability. They introduce no mandatory publisher service, telemetry, hosted identity provider, new protocol authority, or new production dependency.

This is an engineering audit and remediation record, not a certification that every source line is defect-free. The family was mapped across all variants; detailed review concentrated on transport, directory availability, chat delivery, account persistence, wallet counters, device pairing, the shared panel and its desktop integrations. Tests use temporary state and synthetic identities. No funded transaction or fleet deployment was used. Following the owner's authorization, iOS builds through 0.1.31 build 29 were installed and launched on the connected iPhone. The 0.1.20 startup log also confirms relay attachment, push registration and history catch-up; this is not full end-to-end feature validation.

## Most consequential fixes

### High: file-backed wallet counters could treat damaged state as unused keys

The existing Cloud counter reader caught errors and returned zero. The standalone desktop also had a separate implementation of the same persistence responsibility. Treating unreadable, empty or malformed counter state as an unused signing key is unsafe for one-time signatures.

The Cloud implementation is now the shared donor in `walletfile/com/eurobuddha/wallet/FileKeyUses.java`. `cloud/src/main/java/com/eurobuddha/maxima/cloud/CloudKeyUses.java` and `desktop/src/main/java/com/eurobuddha/maxima/desktop/wallet/DesktopKeyUses.java` are thin host adapters. The existing file names, namespaces and properties format remain compatible. Both mirrors are read under the existing JVM/directory and file locks; counts only increase, and reservations are flushed before a leaf is returned. Existing unreadable, empty, malformed, negative or overflowing state now stops the operation. A missing file still permits first use.

The Android donor `walletshared/com/eurobuddha/wallet/PrefsKeyUses.java`, the wallet engine and sibling PandaPools counter implementation/tests were inspected. They use different persistence/remote reservation contracts and were not substituted for this file-backed implementation. The full-node hosts continue using the embedded Minima wallet's counter system.

`walletfile-test/com/eurobuddha/wallet/FileKeyUsesTest.java` runs in both Cloud and desktop: malformed/unreadable state, legacy compatibility, mirror write failure, monotonic imports, exhaustion and concurrent reservations. Three cases fail against the original Cloud implementation; all six pass against the shared implementation in each host. A failed reservation can waste a leaf; it must never reissue one. This does not prove resilience against arbitrary filesystem rollback or malicious local tampering.

### High: account authorisation could diverge from persisted state

`account/src/main/java/com/eurobuddha/maxima/cloud/DevicePairing.java` could acknowledge changes whose file write had failed. It now reports persistence failure and restores the previous in-memory authorisation/pending snapshot. Corrupt existing state fails startup instead of silently becoming an empty account. Bootstrap code reads, writes and consumption also fail explicitly; a code is consumed before authorisation is persisted, so a failed save can require minting a new code but cannot leave that code reusable after a successful authorisation.

The existing backup writer became `account/src/main/java/com/eurobuddha/maxima/cloud/AccountFiles.java`: write a private temporary file in the same directory, flush it, then replace the target. POSIX systems create the temporary file with mode 0600. `AccountBackup` and `cloud/src/main/java/com/eurobuddha/maxima/cloud/Tenants.java` reuse it. The atomic-move fallback applies only when the filesystem reports that atomic moves are unsupported; that fallback is not a power-loss guarantee.

Unauthenticated pending device requests are bounded at 64, labels at 256 characters, and identical retries avoid repeated writes. A valid owner bootstrap code can still pair when the pending queue is full. Five persistence regressions fail against the original implementation and pass with the fix in `cloud/src/junit/java/com/eurobuddha/maxima/cloud/DevicePairingPersistenceTest.java`.

### High: local admin RPC lacked browser-origin checks

`node/src/main/java/com/eurobuddha/maxima/node/AdminRpc.java` still binds to loopback. It now validates the exact local Host/port, rejects foreign or null Origins and inappropriate Fetch Metadata, limits methods to GET/POST, and shuts down its executor on stop. `node/src/test/java/com/eurobuddha/maxima/node/AdminRpcTest.java` uses empty commands and temporary loopback sockets to verify allowed and rejected requests.

This is protection against browser-origin misuse, not local-user authentication: trusted local processes retain access. No admin endpoint was exposed publicly, and no new remote authority was introduced.

## Availability, correctness and resource use

| Finding | Repair and evidence |
| --- | --- |
| A directory mostly/full of local publishers could evict one to admit a best-effort replica. | `core/src/main/java/com/eurobuddha/maxima/core/directory/MlsStore.java` reuses replica-only quota/LRU eviction. With no replica to evict, admission is refused; a one-entry directory reserves its slot for a publisher. Expired reads remove only the exact expired entry, preserving concurrent republication. Three added `MeshReplicateTest` cases reproduce the old failures. Signed proofs, forwarding and other relays remain available. |
| An RPC reply could be followed by a second timeout callback. | `core/src/main/java/com/eurobuddha/maxima/core/rpc/RpcPeer.java` makes expiry conditional on atomically removing the same pending entry and reports the actual requested timeout. Two deterministic `RpcExpiryTest` cases reproduce the race and incorrect timeout reporting. |
| Group receipts arriving during a flush could be erased by clearing the whole queue. | `core/src/main/java/com/eurobuddha/maxima/core/chat/ChatEngine.java` removes only each captured entry if it is still unchanged. Newly queued receipts survive for the next flush. `GroupReceiptRaceTest` reproduces arrivals for both the same and another key during the flush. |
| Concurrent iOS sends could enqueue acknowledgements in a different order from socket writes. | `ParlonsKit/Sources/ParlonsKit/Session/HostConnection.swift` holds the existing send lock through ledger insertion and synchronous submission to `TcpLink`. The Java submission pattern is reused. `AttachedSendTests.swift` sends 256 concurrent synthetic frames through a real local listener with alternating successful/failed acknowledgements. |
| An interrupted HTTP response could leave an Electron operation pending. | Both `main/rpc.js` copies, plus minimaDesk's `main/parlonsapi.js`, reject response-stream errors using the existing minimaDesk `main/net.js` pattern. Five real loopback tests cover interruption, the LF-header parser and UTF-8 request lengths. |
| The optional APNs wake proxy had an unbounded work queue. | `wakeproxy/src/main/java/com/eurobuddha/maxima/wake/WakeHandler.java` uses the existing bounded-executor pattern: eight workers and 256 queued tasks, then HTTP 503 for overload. A blocked fake APNs sender verifies saturation and shutdown without contacting Apple. Wake delivery remains optional. |

## Continuation: RPC lifecycle repair

`MaximaNode.stop()` previously left `RpcPeer`'s reply executor alive and its requests pending. Repeatedly replacing accounts/connections could retain reply workers and callbacks until an unrelated timeout path ran; the old peer also continued accepting calls after its owner stopped.

`RpcPeer` now implements `AutoCloseable`. Registration and close share a lifecycle lock; close marks the peer closed, shuts down queued reply work and conditionally removes each pending entry before failing it. Callbacks execute outside the lock, and a failing callback cannot strand the other requests. New calls reject, new inbound RPCs are ignored, and queued reply tasks check the closed state. `MaximaNode.stop()` owns this cleanup. A write already in progress can still finish; interruption is not a rollback of a remote operation.

`core/src/junit/java/com/eurobuddha/maxima/core/rpc/RpcLifecycleTest.java` exercises the real node owner with synthetic identities and a fake attached transport: pending-call cleanup despite a throwing callback, rejection after stop, and interrupted workers/discarded queued replies. All three tests fail against the original source and pass with the repair. The rebuilt Node jar's RPC/node class files were compared against the tested compiled classes. No relay selection, bootstrap, wire format, service authority or signing algorithm changed.

The full goal remains active. Account worker ownership and keyed-lane shutdown were addressed in the later continuation below. Queue bounds and wake response handling still need further work; the later relay-lifecycle continuation addresses in-flight attachment ownership.

## Continuation: payment retries and counter completion

### Major: rejected payment amounts consumed the retry ID

`ParlonsControl` recorded the `wallet.send` / `wallet.buildsend` request ID before parsing and rejecting a zero amount. Repeating the rejected request then returned `building`; correcting its amount with the same ID could also acknowledge work that was never queued. Both handlers now reuse `chat.pay`'s validation-before-reservation ordering. Valid duplicates still use atomic `putIfAbsent` and cannot queue another payment within the existing retention window.

`cloud/src/junit/java/com/eurobuddha/maxima/cloud/PaymentRetryTest.java` covers both handlers through authenticated local dispatch with a synthetic wallet: repeated invalid requests stay rejected, corrected retries execute once, and valid duplicates remain suppressed. All four cases failed against the original source and pass with the repair. No transaction was signed or broadcast. This repair does not make the existing in-memory payment-ID cache durable across process restarts or extend its 30-minute retention.

### Major: counter adjustments reported success before persistence

`ParlonsControl` immediately reported the requested count while its write was still queued. Both its worker and the `ParlonsCore` adapter swallowed write failures. Fractional and overflowing numeric inputs could also be truncated to a different integer by `Number.intValue()`.

The shared handler now validates an exact integer, keeps the write on the ordered wallet lane and reports success only after the adapter returns and the counter reads back at least the requested value. Adapter exceptions reach the caller. A slow write returns a poll key after the existing Terminal handler's 300 ms completion leash. A single in-flight counter job is retained; same-target retries share it, other targets receive a busy error. Retrying a pending write avoids reading behind that writer's file lock. Ordinary counter reads retain their existing persistence path.

`ParlonsRemote.walletUses()` reuses the Terminal SDK's bounded polling pattern. Pending replies deliberately carry `ok:false` plus an explanatory error so older clients cannot display “Counter raised” before completion. New clients poll the key for up to the polling deadline; an individual network RPC still has its existing timeout/re-resolution behavior. Timeout or interruption does not cancel the monotonic counter write. A later request can refresh the saved value. Only the latest counter job is retained; polling a superseded key returns an explicit refresh instruction.

`cloud/src/junit/java/com/eurobuddha/maxima/cloud/WalletUsesTest.java` covers persisted success, write failure and recovery, blocked-write retries/polling, and invalid numeric admission. The initial four regressions all failed against the original source. Final tests also verify that a retry avoids a blocked counter read and a repeated completed target causes no extra write. The adapter and SDK changes were reviewed with the portal caller, compiled in the affected host builds, and checked for propagation in both jars. End-to-end device/network counter adjustment was not exercised.

### Review and propagation

The two major findings above are repaired. Review of the final changes found no additional blocking issue in this scope. Existing authentication, raise-only persistence, key custody, wallet implementation selection and broadcast paths remain intact. The repairs use the existing account module; no runtime service, authority, dependency or protocol discovery rule was added.

Payment repair commit: `758dd5aeb6a678642d2eaf2a2722bcea2b3abb65`. Counter repair commit: `bfc3056fb85a398dcac2ce4cdc1a270bb88bee69`. The current Node and Cloud jars contain byte-identical copies of the tested controller, account owner and SDK class files. Both Electron integrations fetch the Node dependency through their existing checksum-verifying scripts.

## Continuation: account worker shutdown

### Major: stopped accounts retained executors and queued work

`ParlonsCore.shutdown()` stopped its maintenance pump, local panel and node, but left the control channel's send/media/call/push/console executors, optional wake worker, wallet upkeep worker and host attachment worker open. `SerialLanes.shutdownNow()` stopped only the underlying pool: its own per-key queues survived, so a running task that caught interruption could continue into queued operations. A refused or null task could also leave a key with no runner and silently strand later submissions.

`ParlonsControl` now owns an idempotent `close()`: refuse new requests (including pairing), stop owned executors, discard queued keyed work, stop wake admission, detach the local event sink and release transient caches. Pending counter/console responses report closure and possible completion of running work. Push/call callbacks handle closure races. Profile re-announcement uses the existing send lane instead of creating an unmanaged thread per rename.

`ParlonsCore.shutdown()` calls this cleanup and stops wallet/host workers. Wallet opening now uses the existing wallet executor and checks whether the account is still running before publishing readiness or starting upkeep. Startup and shutdown are serialized; a closed runtime cannot restart or adopt another relay, and a failed startup invokes cleanup. This serialization means shutdown waits for an already-running startup to return; its existing network connection leashes still apply.

`SerialLanes` guards queue admission and closure together, clears per-key queues on stop, rejects every later submission, and rejects null tasks before mutating the lane map. `WakeProxyClient` also serializes admission with close, stops its executor and preserves interruption. The wake proxy remains optional and device-selected.

The three added `SerialLanesTest` cases and the first two `AccountLifecycleTest` cases all failed against the original code. Three additional account tests verify that a queued payment never reaches the synthetic wallet after stop, a delayed wallet open does not publish readiness/start upkeep, and a stopped runtime cannot restart or adopt a relay. Existing wake, payment and counter tests remain green. The full family gate passes **244 JVM tests** and **63 wire vectors**, plus Android, portal and JVM packages. Tested class files were compared byte-for-byte inside both account-host jars.

### Limits and next work

Already-running operations may finish; interrupting a worker is not a rollback of signing, broadcasting or a console command. This repair does not add durable queue replay or prove that every in-flight socket has stopped when shutdown returns. Source review found a separate `HostPool.attachOne()` / `closeAll()` race: an attachment finishing after the active-host snapshot can be inserted into the closed pool. The following transport continuation reproduces and repairs that race in Java and Swift. The account's still-unbounded work queues and the wake client's unused response-body buffering also remain under review.

Review verdict for this worker-ownership change: approve within the stated scope. Relay choice, independent operation, wire format, custody and signing algorithms are unchanged. The whole-family audit remains active.

## Continuation: pending relay attachments

### Major: closed pools could acquire new connections

Java and Swift `HostPool.closeAll()` closed only connections already in the active map. An in-flight handshake could finish afterward and insert its socket into the closed pool. Both implementations also allowed concurrent attempts for the same host, replacing the first connection without closing it. Java `HostConnection.attach()` could create a fresh socket after that connection instance had already been closed.

Both pools now register pending connections under a lifecycle lock before dialing. Only one attempt per host is admitted; completing an attempt can publish it only while it still owns that registration and the pool remains open. Shutdown drains active and pending connections, refuses future attempts and closes sockets outside the pool lock. Deliberate detach also cancels pending work, without counting the cancellation as a relay failure. Java connection instances own one socket and cannot reopen after closure; reader startup also checks closure.

The implementation reuses the Java account/RPC closure pattern, Swift's existing remove-under-lock/close-outside-lock pattern, and the existing `FakeRelay` / `AckRelay` fixtures with held greetings. Four Java tests and three Swift tests failed against the original sources. The final suites add deliberate-detach and Swift duplicate-attempt cases, giving five lifecycle tests in each language. The Java detach case also verifies immediate retry. Fixtures use loopback and synthetic identities only.

Validation: **249 JVM tests**, **30 Swift release tests**, **63 wire vectors**, Android/portal release APKs and JVM packages passed. The iOS application and notification extension compiled for the generic simulator; both bundle plists report **0.1.16, build 14**. Six tested transport class files match byte-for-byte inside Node and Cloud. [GitHub CI](https://github.com/eurobuddha/maxima/actions/runs/34382690319) passed.

Java source commit: `be729c1b48117ff1eb7ddcd421a4a81e20a83537`. Swift source commit: `91d20a39fc0f2bda9d4340a92bcd3dbcd82b3527`. Node **0.2.73** is published and checksum-verified in both desktop integrations. Review found no additional blocking issue in this patch. Network I/O and socket closure remain outside the pool lock; relay selection, independent host choice, discovery, protocol, signing and custody are unchanged.

This repair covers pool shutdown and deliberate detach. It does not establish complete Swift task-cancellation correctness or eliminate every possible late connection callback during reconnect. Those paths, worker queue bounds and wake response handling remain open audit work. No physical-device or sustained multi-relay churn run was performed.

## Continuation: Swift cancellation and transport cleanup

### Major: cancellation could return success and retain pending work

`ParlonsKit/Sources/ParlonsKit/Session/TcpLink.swift` used unstructured tasks for `withTimeout`, but its comment incorrectly claimed caller cancellation was inherited. Cancelling a caller did not promptly release it or cancel cooperative work, and an already-cancelled caller could still run the operation and receive success. A completed operation also left its timer sleeping until the deadline.

The existing `ResumeOnce` now owns the operation/timer handles and records the first completion even before its continuation is armed. Completion cancels and releases the owned tasks before resuming the caller. Cancellation and deadlines still return without waiting for a body parked on a non-cooperative continuation; resource owners remain responsible for releasing that continuation.

`RpcPeer.call` removes its waiter and cancels its detached send task when the call exits. Registration and cleanup use the same lock, and registration checks cancellation. Attached sends retain the existing ACK tombstone after cancellation, preventing a late ACK from answering another send. Cancelled relay attachments cannot enter the pool or penalise a relay. Cancelled RPCs are propagated instead of entering the ordinary resolve-and-retry path, and the maintenance pump checks cancellation after sleep.

### Major: abandoned TCP reads could remain on a reusable stream

A timed-out `TcpLink.readExactly` returned while its receive continuation could still consume bytes later. It now closes on error/cancellation; the abandoned body checks cancellation before appending bytes. Connect admission, readiness and closure share a lock, and a link instance starts once. Async write cancellation closes its socket. These changes do not cancel an already-sent remote command or reverse its effects.

### Review, verification and propagation

Four helper regressions failed against the previous source; all now pass. Tests also cover prompt RPC waiter/send cleanup, cancellation before a relay greeting, a cancelled send followed by a differently acknowledged send, read-timeout closure, and the existing non-cooperative deadline behavior. The final full Swift suite passed **39 tests**; one additional focused closed-link retention test passed, giving **40 passing tests** across the two runs. The retention hypothesis was not reproduced, so no handler-retention source change was made. iOS application and notification extension builds passed for the generic simulator, both reporting **0.1.17, build 15**. No physical device or APNs delivery was exercised. There is no GitHub workflow in the iOS repository; validation was local.

Source commit: `5c9e75301f162b6485f87aa8e67753ef4247c2ba`. Review verdict: approve for this cancellation/cleanup scope. The Swift package is compiled into both the application and notification extension. Java counterparts were inspected: they use blocking socket timeouts/try-with-resources and shared RPC expiry rather than this Swift helper. At this stage both desktop integrations retained the already-verified Node **0.2.73** (updated to **0.2.74** in the Java continuation); no Swift code is bundled into those Java/Electron artifacts. Wire format, relay selection, directory voting and key custody are unchanged; no runtime authority or service was introduced.

Further source review found Java `HostConnection.send` catches `InterruptedException` through a generic catch and loses the interrupt flag; this is reproduced and repaired in the following shared-core continuation. iOS `SessionController` also does not own/cancel every in-flight connect attempt before background/reconnect transitions. Shared resolver-task cancellation, stale connection callbacks, dead-link reconciliation and sustained churn remain open review work. The full goal remains active.

## Continuation: Java send interruption

### Major: interrupted sends lost the worker's stop signal

`core/.../net/HostConnection.send` caught interruption of its ACK wait through a generic exception handler. `CompletableFuture.get` clears the thread interrupt status when it throws, so the owning worker could continue as if no stop had been requested. An already-interrupted thread could also enqueue/write a frame before reaching that wait. `MaximaSender.send` could invoke an attached transport or begin a fresh-socket fallback despite an existing interrupt.

Attached sends now check interruption before building/enqueuing and again after acquiring the send lock. Interrupted ACK waits restore the flag and retain the existing waiter position, so a late ACK cannot answer the next send. The shared sender checks interruption before invoking an attached transport and before falling back to a fresh socket. This reuses the interrupt-preservation pattern already present in `WakeProxyClient`; no protocol or routing change is involved.

All four `SendInterruptionTest` regressions failed against the original source. They cover the restored flag, late-ACK ordering, no frame from an already-interrupted sender, refusal before invoking an attached transport, and refusal before fresh fallback. The existing loopback `FakeRelay` now exposes a message hook, following the held-ACK server tests. Synthetic identities and sentinel transport inputs avoid any real-account send.

Interruption is cooperative admission/ACK-wait cancellation, not a rollback: a frame already written can still be delivered. This does not make a blocking Java socket connect/write immediately interruptible; the owning connection's closure and existing socket timeouts still govern those operations. Review found no blocking issue in this change. The equivalent Swift cancellation handling is included in iOS **0.1.17**.

Validation: **253 JVM tests**, **63 wire checks**, Android/portal release builds and all JVM packages passed. Seven tested sender/connection class files match byte-for-byte inside Node and Cloud. [GitHub CI](https://github.com/eurobuddha/maxima/actions/runs/34385839013) passed. Source commit: `81283bfc6d2053dd5e51886b13b080db3ba9eef6`. Node **0.2.74** is published and checksum-verified in minimaCore Desktop **0.16.49** and minimaDesk **0.7.33**; their pins are committed and pushed. No installer release or installed-app update was performed.

## Continuation: stale relay callbacks and closed-link maintenance

### Major: an old connection's death callback could remove its replacement

Java `MaximaNode.onDead` and Swift `DeviceNode.onDead` detached by host name alone. A delayed notification from a retired connection could therefore close a healthy replacement for that same relay; Java could also cancel a replacement handshake. Java reconciliation acted on a snapshot but detached unconditionally, exposing the same replacement race. Swift maintenance checked staleness but left a closed connection in the pool if no death callback arrived.

The pools reuse their existing lifecycle locks and remove-under-lock/close-outside-lock pattern. Death notification now reaps only an already-closed active connection and leaves pending attachment ownership intact. Maintenance conditionally removes the exact connection it inspected. Swift also checks attachment state, following Java's existing health check, and ignores stale notifications before running device-level death/refill callbacks. Deliberate detach retains its existing ability to cancel pending work.

`HostReapingTest.java` and `HostReapingTests.swift` each add three regressions using the existing loopback relay fixtures and synthetic identities. Two cases in each language fail against the original source; the third verifies that a currently closed connection is still reaped. No bootstrap, relay preference, discovery, protocol, signing or custody rule changed.

Validation: **256 JVM tests**, **43 Swift release tests**, **63 wire checks**, Android/portal release APKs, JVM packages and iOS simulator/device builds passed. The initial JVM family run had one greeting timeout in the existing `HostPoolShedTest`; focused and full reruns passed without changing that test or its timeouts. The cause of that isolated timeout was not established. Ten tested node/pool class files match byte-for-byte inside Node and Cloud. All 11 panel assets in both jars match source. [GitHub CI](https://github.com/eurobuddha/maxima/actions/runs/34388179225) passed.

Java commit: `3ce743f5fd94ff9c6e17bf2057988f00d0ed1394`. Swift commit: `d20b068eb178feb755ee7b4624103f35d97e5e4e`. Node **0.2.75** is published and checksum-verified in minimaCore Desktop **0.16.50** (`b586aecddc31f5044df5b73a439b319a25a07327`) and minimaDesk **0.7.34** (`4f9b1d281f17b45e0553ca29fc29ad9838c7ac87`). All four source changes are committed and pushed.

The owner-authorized iPhone installation upgraded the existing app from 0.1.12 build 10 to **0.1.18 build 16**. The signed device build, strict signature verification, device-reported installed version and launch succeeded. App and notification-extension bundle versions match. No messages or funded operations were initiated; phone UI, live-account connectivity and APNs delivery were not verified.

Review verdict: approve within this scope. Remaining review targets include session ownership across iOS background/reconnect transitions, cancellation of shared resolver work, load-shedding callback identity and sustained relay churn. These fixes do not establish complete connection-lifecycle correctness.

## Continuation: iOS foreground session ownership

### Major: retired connection attempts could publish state and keep work alive

`support/parlons-ios/App/Sources/Session/SessionController.swift` started connection tasks without retaining them. Backgrounding did not cancel a pending connection, retries or path monitoring. Concurrent reconnects could create separate remotes, a failed setup did not close its local remote, and an old failure could overwrite a newer connected state. Late push/path/heartbeat/refill/history completions also lacked consistent session ownership checks.

The repair adapts the generation and failure-cleanup pattern from the Android portal's `portal/src/main/java/com/eurobuddha/maxima/app/portal/CloudSession.java` and the tested stale-reply checks in minimaCore Desktop's `renderer/app.js`. Swift retains its existing connection setup and network methods. A small injected setup boundary supplies account lookup, remote creation, establishment and metadata persistence for isolated tests; production uses the existing Keychain, `ParlonsRemote`, `PushService` and seed selection.

The controller now owns connection, retry, heartbeat, refill, path-debounce and bounded retirement tasks. Invalidation retires the generation before cancellation/closure, closes pending remotes, stops the path monitor and clears stale connection indicators. Concurrent reconnects share the current connection attempt. A successful attempt publishes only while current; failed attempts close their remote. History paging remains part of the owned task after publication, and checks the generation before applying pages or advancing the cursor. Push, path, heartbeat and refill callbacks reject retired sessions. Simultaneous refills are coalesced. Foreground startup cancels/closes unfinished retirement work; unpairing performs the same cleanup before clearing local account state.

Five `App/Tests/SessionLifecycleTests.swift` regressions fail against the original lifecycle logic with the injected setup boundary: late success after background, reconnect while backgrounded, overlapping reconnects, pending RPC cleanup after setup failure, and an old failure overwriting a new session. Two additional cases verify retirement of push callbacks and cancellation of history catch-up after publication. All seven pass after repair. Tests use synthetic identities and controlled async gates; their RPC fixture intercepts the send path without contacting a relay or account.

The full app run also found an outdated `CatchUpTests.swift` fixture that expected a raw manifest to render as a photo. The existing Swift `ChatMediaTests.swift` and Java `ChatMedia.wrap/preview` establish that real messages use the SOH media marker. The app test now exercises that format, voice-note metadata hiding and unchanged plain JSON text. Production preview behavior was retained.

Validation: **8 app tests** pass in the iOS simulator in Release configuration with `ENABLE_TESTABILITY=YES ONLY_ACTIVE_ARCH=YES`. The initial Release test-build failure was caused by `@testable` importing a module without testability enabled; no tests ran in that attempt. Final app/NSE device builds and strict signature verification pass. No shared package source changed after the previously passing **43 Swift package tests**, **256 JVM tests** and **63 wire vectors**.

Source commit: `0e24a409ace7ea6dd2a4975f86e9ded62c8995a7`, **iOS 0.1.20 build 18**. Committed, pushed, installed and launched on the owner-authorized connected iPhone; the device confirms the version and both bundle plists agree. The version label reads from the bundle automatically. Both desktop pins and local jar checksums were reverified at Node **0.2.75**: this app-controller change is not bundled into Java/Electron, and the notification extension shares only the unchanged package.

The physical phone's startup log confirms two attached relays, successful account push registration, history catch-up, summary fetch and APNs-token registration. Relay/warm connection took about 1.6 seconds, but a subsequent ping took about 20.9 seconds and a push-registration retry required re-resolution; registration completed about a minute after startup. The source of that delay is not established and remains a performance investigation. Registering an APNs token is not proof of notification delivery. No messages or funded operations were manually initiated, and no physical Wi-Fi/background stress sequence was performed.

Review verdict: approve within this scope. No protocol, directory, relay-selection, signing, custody or mandatory service change was introduced. Cancellation cannot recall a request already sent to the account, including a retirement liveness update. Shared resolver-task cancellation and internal `ParlonsRemote`/RPC lifecycle ownership still require review; generation checks prevent their late results from publishing this retired app session but do not prove every underlying task stops immediately. Swift 6 concurrency warnings in the existing notification-extension callback are also outside this patch.

## Continuation: Swift RPC peer shutdown

### Major: a stopped device node continued accepting and retaining RPC work

`ParlonsKit/Sources/ParlonsKit/Session/DeviceNode.swift` closed its relay pool but did not close its RPC peer. `RpcPeer` had no closed state: retained references could submit calls and dispatch inbound requests after stop, pending calls remained until their deadlines, service handlers could launch replies after their owner stopped, and reply tasks had no lifecycle owner. `ParlonsRemote.close()` failed pending callbacks with a generic transport error, allowing its ordinary RPC recovery path to retry an intentionally closed session.

The repair ports the existing Java `core/.../rpc/RpcPeer.java` lifecycle and its `RpcLifecycleTest.java` cases. Swift reuses its own lock-based pending registration, task cancellation, `Box` and `ResumeOnce` fixtures. The RPC peer now owns its send, service and reply tasks; close marks admission closed under the same lock, clears pending calls and routing references, then cancels tasks and resumes callers outside the lock. Repeated closure is harmless. Address/attached-sender access is synchronized, and setters cannot revive a closed peer. A late service completion cannot enqueue replies after closure.

`DeviceNode.stop()` closes the peer before its pool. `ParlonsRemote` preserves the typed local `RpcError.closed` through both RPC attempts, and checks peer state before looking up the live address. Closing a remote now uses the node's full closure instead of a generic pending-call failure. Ordinary transport failures retain their existing recovery behavior. Cancellation raised during the recovery attempt is also preserved instead of being replaced by the first transport error.

Five new `RpcLifecycleTests.swift` cases fail against the original sources: pending-call cleanup, rejection of new calls, rejection of inbound work, late-handler reply suppression, and active-reply cancellation. The open-peer dispatch/reply case passes before and after. Two further tests verify closure through `ParlonsRemote`: no new RPC recovery after close and no retry/address resurrection for a pending call. All fixtures use synthetic identities and intercepted transports; no relay or account is contacted.

Validation: the complete Swift package suite passes **51 tests**, and the simulator application suite passes **8 tests**. The application and notification extension build as **0.1.21 build 19**; strict signed-bundle verification, installation and launch pass, and the connected iPhone reports that version. Commit `0dac546c59daf6d4ef485e3140f3519e048cb763` is pushed. Both iOS targets compile the repaired package. Java already contains the corresponding RPC peer shutdown, so no Java patch or new Node release is needed; both desktop **0.2.75** Node pins and jar checksums were reverified.

Review verdict: approve for RPC peer ownership and SDK closure propagation. Relay selection, signed discovery, wire format, account custody and optional wake infrastructure are unchanged. Already-running handlers/writes may finish; cancellation cannot roll back a remote operation. This patch does not bound the Swift inbound/reply task population, stop every already-running shared resolver, or establish the cause of the earlier physical-phone startup delay. Shared `resolveTask.value` cancellation and remote pump/raw-send ownership are addressed in the following continuation; work bounds and latency remain open audit work. Physical validation of this build covers installation and launch; the previous build's startup log is recorded separately above.

## Continuation: Swift shared resolution and raw-send ownership

### Major: cancelled resolution waiters and closed remotes retained work

`ParlonsKit/Sources/ParlonsKit/Account/ParlonsRemote.swift` awaited an unstructured shared task directly. Cancelling its caller did not release that waiter or stop an unneeded lookup. Closing the remote neither cancelled the shared lookup nor owned raw sends using fresh sockets. A delayed lookup could subsequently publish an address, and a closed remote could resolve another bare address.

The repair directly reuses `ResumeOnce` from `Session/TcpLink.swift` for independent waiter completion and owned raw-send cancellation. A lifecycle lock protects admission, account/address publication and task handles. Callers resolving the same account share one lookup; cancelling one caller preserves other waiters, while cancelling the last retires and cancels the lookup. Completion publishes only when that exact lookup still owns the slot and account. Different accounts cannot join one in-flight lookup. Close drains the waiters and raw sends, cancels the lookup and maintenance pump, clears the live address, and stops the node. Pump registration and closure now share the same lock. Cancellation and the typed closed error pass through directory fallback unchanged.

The existing lookup ladder, anchor-first discovery, requirement for two agreeing fallback directories, nonce/key checks and wire formats are retained. The inspected Java account remote and FreezePeach image loader do not provide Swift's shared cancellable waiter contract; the smallest added coordinator wraps the existing lookup and completion primitives. This Swift package change is consumed by both the iOS app and its notification extension. No JVM implementation or panel asset changed, so the desktop dependencies remain Node **0.2.75**; both pins and local jar checksums were reverified.

Four negative regressions fail against the old coordination with an injected lookup boundary, including a real loopback fresh socket held before its greeting. The repaired suite also checks that cancelling one waiter preserves another, retired results cannot overwrite a replacement, and different accounts cannot share a lookup. All **58 Swift package tests pass**. Tests use synthetic identities and local fixtures.

Review verdict: approve for shared resolution, raw-send and maintenance-task ownership. Cancellation cannot undo a completed remote operation or force a non-cooperative lookup to finish; identity checks prevent a retired lookup from publishing. This does not establish the cause of the earlier iPhone startup delay, bound all RPC work, or verify notification-extension concurrency. The **8 application simulator tests pass**, and both application and notification-extension bundles build as **0.1.22 build 20** for simulator and iPhone. Strict signature verification passed; installation and launch succeeded, and the connected iPhone reports that version. Physical validation here covers installation and launch. Commit `17c98ef33d3ba18ad04d5641690491fad5e5d1ca` is pushed.

## Continuation: iOS relay choices in the notification extension

### High: background notifications could disregard the built-in relay switch

`App/Shared/CatchUp.swift` used `SeedStore` backed by `UserDefaults.standard`. The app and its notification extension have different private preference domains. An extension therefore saw its own empty relay settings, enabling the compiled-in bootstrap list even when the containing app had saved custom relays and disabled that list. Apple's [app-extension storage documentation](https://developer.apple.com/library/archive/documentation/General/Conceptual/ExtensibilityPG/ExtensionScenarios.html) confirms that sharing preferences requires an app-group suite; merely compiling the same source into both targets does not share their private defaults.

The existing `SeedStore` operations now use `App/Shared/SeedStore.swift` and the already-entitled `group.com.eurobuddha.parlons` suite. Relay composition, exclusions and last-seed rules directly reuse `ParlonsKit/Sources/ParlonsKit/Session/SeedRelays.swift`; the Android portal store and shared Java seed tests were inspected for parity. Only the containing app migrates legacy settings. It publishes the user relays, exclusions and built-in switch as one record, and subsequent app launches preserve that record rather than restoring stale private settings. Reads compose one complete snapshot, and app mutations are protected by a lock. Missing, unavailable or malformed shared data yields no relays; background catch-up returns before creating an identity or opening a session in that case. The extension never assumes built-in consent while waiting for the app to migrate.

Four regression cases fail against the old private-domain behavior with an injectable defaults boundary. All **seven preference tests** pass after the fix: migrated custom-only operation, extension-before-app ordering, observing edits/exclusions, relaunch preservation, malformed records, unavailable storage, and the existing last-seed rules. The full application simulator suite passes **15 tests**. Fixtures use disposable preference suites and synthetic relay names; they do not modify the owner's relay choices. The shared Swift transport package is unchanged from its previous **58-test** passing run.

Review verdict: approve for relay preference scope and migration. Gain: the background and foreground paths honour the same operator choices and avoid futile catch-up when no authorised configuration is available. Sovereignty: no bootstrap service becomes required, and no authority or network protocol changes. The local storage repair is built into both iOS targets; the owner's relay sources remain replaceable. Risk: an extension launched after upgrading but before the app first migrates cannot fetch a rich banner; it retains the ordinary content-free notification instead of choosing another relay source. The app initializes the shared record on its next use of relay preferences. This does not resolve the separately identified notification-completion warnings or establish a total catch-up deadline.

Application and notification-extension builds pass as **0.1.23 build 21**. Strict signature verification and inspection of both signed app-group entitlements pass. Installation and launch succeeded; the connected iPhone reports that version. Source commit `2ca44d9b9dff444673c68ddd8728a5ffdff2cec6` is pushed. Automatic approval review rejected copying both the old private preferences and the new app-group preferences from the device because they could contain private settings without explicit permission. No preferences were copied. Consequently, actual stored migration on the owner's phone and real extension delivery are not claimed as verified; isolated migration tests, signed target configuration, installation and launch are the evidence for this pass.

Both desktop Node **0.2.75** pins and jar hashes were reverified. Their JVM account/relay settings do not use iOS application-private preferences, and no JVM or shared panel code changed in this continuation; no desktop dependency release is necessary.

## Continuation: notification expiry and background completion

### Major: expired refreshes completed twice and notification expiry lost fetched previews

`App/Sources/Background/BGRefresh.swift` independently called the system completion from its worker and expiration handler. The worker was launched before that handler was installed, so immediate expiry could still start catch-up. In `App/NSE/NotificationService.swift`, expiry discarded the worker's already-fetched preview, and handler/content state was accessed without a shared synchronization boundary. The compiler flagged concurrently captured mutable state. Apple's [notification-extension guidance](https://developer.apple.com/documentation/usernotifications/modifying-content-in-newly-delivered-notifications) requires returning available content immediately when extension time expires.

The repair reuses `ResumeOnce` in `ParlonsKit/Sources/ParlonsKit/Session/TcpLink.swift`, exposing a callback consumer while retaining its existing lock, early-result handling and owned-task cancellation. Continuation consumers use that same callback path. Completion clears its stored consumer and task handles, cancels owned work, then invokes the consumer synchronously outside the lock. Background refresh installs expiration before admitting its worker, and normal/expired completion share that single guard. The system expiration handler is cleared when completion is delivered.

`App/Shared/NotificationDelivery.swift` owns one notification's handler, latest arrival and count. Progress and handler retirement share a lock. Expiry delivers the best fetched preview immediately, or the original content if no message was fetched, while cancelling the owned task. Late arrivals and repeat finishes cannot change or redeliver that banner. The extension retains only the current delivery; replacement, expiry and teardown retire that operation. Formatting still directly uses `ChatMedia.preview` and the existing sound, conversation grouping and peer metadata. `CatchUp.Arrived` and its arrival callback now explicitly support transfer across tasks. The previous mutable-capture warnings are absent from both final target builds.

Three regression cases fail against the original completion behavior with injected worker boundaries: duplicate refresh completion, admission after immediate expiry, and loss of the fetched preview. The complete **seven-case background completion suite** also covers concurrent/repeated expiry and late arrivals, normal media preview formatting, cancellation before system completion, and normal refresh success/failure. Two package tests cover callback early delivery/late-work rejection and reentrant completion. All **60 package tests** and **22 application tests** pass on the final relevant sources. Tests use synthetic content and held local workers; they do not send APNs notifications or contact an account.

Review verdict: approve for one-time completion, preview ownership and expiry cancellation. Gain: retired tasks cannot report a second system result, and expiry preserves useful content already fetched. Relay choice, independent discovery, end-to-end account transport and the optional nature of the wake infrastructure are unchanged. This is a local lifecycle repair with no new service or runtime dependency. Cancellation is cooperative and cannot undo an operation already completed. The existing `CatchUp.run` time limit still wraps connection setup only; total catch-up duration, cursor/storage effects and post-cancellation work require the next dedicated review. This pass does not claim to resolve those issues.

The application and notification extension build as **0.1.24 build 22**. Strict signature verification, authorized installation and launch succeed, and the connected iPhone reports that version. Commit `809ee60d0d52781f8edaf8390a2d700d218cc0ba` is pushed. No device preferences were read in this continuation; the earlier permission request remains pending. Physical verification covers installation and launch, not real background scheduling or APNs delivery. Both desktop Node **0.2.75** pins and local jar hashes were reverified. These Apple-framework lifecycle paths are confined to the two iOS targets, so no JVM or desktop dependency change is needed.

## Continuation: the full background catch-up deadline

### Major: paging and preparation escaped the budget; cancelled shutdown still checkpointed and notified

`App/Shared/CatchUp.swift` placed its timeout around connection setup only. Preparation, history paging and the final `registerPush(live:false)` could outlast the advertised budget. An already-cancelled caller still entered identity preparation. The final request also swallowed cancellation with `try?`, after the cursor had already advanced, and could then post a local notification and return success.

The repair directly reuses `withTimeout` and `ParlonsRemote.close`, adapting the pending-session ownership pattern from `SessionController.ConnectionSetup` and its teardown. An app-specific `Setup` isolates real storage/network boundaries for tests; live execution retains the existing Keychain, shared relay preferences and authenticated account RPC calls. One deadline now wraps preparation, connection, paging, final offline registration and completion effects. The owner closes an adopted remote on timeout/cancellation/exit and rejects a remote returned by late preparation. Cancellation checks after asynchronous boundaries reject late pages and notifications. The cursor is staged until the final request has returned and cancellation has been checked. Ordinary failure of that best-effort request still permits reporting messages that were fetched successfully. Paging limits, incoming/own-message filtering, notification-consumer behavior and relay discovery rules remain unchanged.

All four negative regressions fail with the old algorithm behind the same injected boundaries: held paging, cancelled offline registration, preparation after caller cancellation, and held preparation. The final **eight-case catch-up suite** also covers a held final offline request, successful multi-page checkpointing with own-message filtering, an arrival consumer suppressing a duplicate local notification, and ordinary offline-request failure. All **30 application tests** pass. Tests hold synthetic sessions and record injected effects; they do not read the owner's Keychain, modify real cursors or send notifications. The Swift package source is unchanged from its previous **60-test** passing run.

Review verdict: approve for whole-operation deadline coverage and late-result cleanup. Gain: a stalled page/final request no longer retains the background caller beyond its timeout, and cancellation cannot continue from those awaits into a cursor update or notification. Sovereignty: shared user-selected relays, end-to-end transport and optional wake infrastructure are retained; no service, network authority or runtime dependency is added. Cancellation cannot interrupt synchronous preparation/storage already executing or undo an effect already in progress. Cross-process account/cursor coordination, paging under a changing history, the existing page cap and cache durability remain separate review targets; this repair does not claim transactional notification delivery.

The application and notification extension build as **0.1.25 build 23**. Strict signature verification, authorized installation and launch pass; the iPhone reports that version. Commit `3912579e3d08caa3ef32933192f81b901ba7a965` is pushed. Physical validation is installation and launch, not a real APNs/background scheduling exercise. The earlier phone-preferences permission remains pending and no such read was attempted. Both desktop **0.2.75** Node pins and local jar hashes were reverified. Their JVM account implementations do not run this iOS background helper, so no desktop dependency change is required.

## Continuation: cached refresh ownership and atomic snapshots

### Major: late reads could restore retired or superseded data

The three cache-writing iOS views accepted every successful asynchronous result. A slow response could overwrite newer data, return rows from a replaced session, or refill the cache after session retirement and clearing. `SessionController.refreshCached` now reuses the existing foreground, session-generation, cancellation and remote-identity checks, with a per-snapshot request identity. Only the latest eligible read writes the cache and returns rows for display. Retirement clears outstanding identities. Failed newer reads retain the saved snapshot and still supersede older pending results; different cache names remain independent. This rejects results rather than coalescing or cancelling the underlying fetches.

`ChatView`, `ChatsListView` and `ContactsListView` use this one helper. The three original lifecycle/overlap regressions fail against the old behavior extracted behind the same injected cache/network boundaries: six failed assertions. All six final `CachedRefreshTests` pass, also covering active publication across separate cache keys, failed-newer-read preservation and non-cooperative cancellation. The tests use isolated temporary caches and synthetic sessions; retirement followed by clearing exercises the relevant unpair ordering without invoking Keychain deletion.

### Cache write contention and fewer filesystem operations

`CacheStore.write` wrote atomically to a fixed `name.tmp`, then replaced `name.json` separately. Each store had its own lock despite sharing that staging filename. It now writes atomically to the final URL, directly reusing the existing Foundation operation and the pattern in `support/freezepeach-ios/App/Sources/Store.swift`. Foundation owns the temporary path; the extra replacement is removed. Existing JSON validation, reads, removal and clearing remain intact. The notification extension currently does not write this cache, so its former writer description was corrected.

The initial suspicion that a first write fails was **not reproduced**: all four initial disk tests pass against the old source. The separate eight-writer/320-write stress run stalled in `FileManager.replaceItemAt` / `__renameatx_np` on macOS. Its sampled stacks are saved in `/private/tmp/parlons-cache-concurrent-sample.txt`. Graceful and forced termination were requested; the old XCTest process remained kernel-blocked at the last check, while its parent was stopped to release the SwiftPM build lock. This is an observed local stress failure, not a proven iPhone production deadlock. The same stress case completes in **0.084 seconds** with the repair. All five disk cases pass, including reopening, replacement, writing after clear and rejecting invalid JSON without replacing saved data. These checks do not certify power-loss durability or provide cross-process ordering for clear versus write.

Review verdict: approve for the scoped cache repair. The complete release suites pass **65 Swift package tests** and **36 iOS app tests**. The app and extension compile as **0.1.26 build 24**, their bundle versions match, strict signature verification succeeds, and the authorized iPhone install and launch succeed. Device metadata confirms that version; its existing top-left label reads the installed bundle version automatically. Commit `087c44133c6ad781bdb5241a54b6630c41314f04` is pushed. Physical validation remains installation/launch rather than live-account reconciliation or APNs delivery.

Relay choice, account authority, wire behavior and optional wake services are unchanged. At this stage both desktop Node pins remained **0.2.75** and both jar hashes matched that verified release. These Swift cache paths have no JVM counterpart to propagate. The minimaCore working tree also had unrelated changes reporting 0.16.52; those were preserved. Its audited committed version was 0.16.50 at this stage; the later relay-shedding continuation records the next dependency update. The pending permission to copy phone preferences was not retried. Other asynchronous UI effects, paging reconciliation and cross-process account/cursor coordination remain review targets.

## Continuation: relay load-shedding handoff

### Major: a delayed advisory could detach a different connection

`HostPool` queued relay shed callbacks using only the host address. A callback from a retired connection could therefore act on a newly attached connection at that address. A shed already waiting for an alternative greeting could also detach the newer connection or cancel its pending handshake. The final detach did not recheck a changed preferred relay or whether the alternative was still attached.

The callback now carries its originating `HostConnection`. Shed admission and the existing per-host time allowance are checked under the pool lifecycle lock; stale callbacks do not consume the current connection's allowance. The candidate loop checks ownership before another dial. Handoff reuses `detachCurrent`'s conditional removal, extended to require the original eligible source and a currently attached alternative. Preferred-relay changes share that lock. Socket closure and completion callbacks remain outside it. The existing client-side random candidate draw, three-attempt cap, 30-minute advisory interval and preferred-relay policy are retained.

All five initial `HostShedLifecycleTest` regressions fail against the original source: queued stale advisories, source replacement during a slow alternative attach, a new source handshake during that attach, a source becoming preferred, and an alternative closing before handoff. They pass with the repair. The final queued-callback case additionally verifies that a fresh advisory still works after the stale one is ignored. Existing `HostPoolShedTest`, `HostPoolLifecycleTest` and `HostReapingTest` remain green. These fixtures reuse `FakeRelay` and synthetic identities on loopback; they do not connect to public relays or copy an account.

Review verdict: approve for this lifecycle repair. Gain: stale or invalid handoffs no longer remove current reachability or waste a new dial from an already-retired callback. Sovereignty: shedding remains optional advice with no destination supplied by the relay; the client chooses from its own candidates. No central service, new authority, dependency or wire format is introduced. The check establishes local attachment state at handoff, not future network availability. An alternative already attached when the original source changes is retained; this can leave an extra attachment, and the pool target is not a strict concurrent population cap. Queue bounds/coalescing for advisory work and cancellation of an already-started alternative dial remain further hardening work.

The complete local gate passes **261 JVM tests**, **63 wire vectors** and **18 panel/desktop JavaScript tests**. Both Android release APKs build with the new versions below. All three `HostPool` class files in the Node, Cloud and relay jars match the tested compilation byte-for-byte; all eleven panel assets in Node and Cloud match source. The exact shared-code commit `5d8683637e2496a36c869f15de77fa7f83b42dbe` is pushed and its [GitHub CI run](https://github.com/eurobuddha/maxima/actions/runs/34401007748) succeeds.

The checksum-verified **Node 0.2.76** dependency is published and fetched through both desktops' existing scripts. minimaCore's isolated dependency commit is **0.16.51**, `e6d1573d39d3bb0a816064a4391144b0e079dca5`; its unrelated working-tree **0.16.52** changes, including manifests, remain intact. minimaDesk is **0.7.35**, `5edca4732bc288b72b33742019977298bbbf865a`. Both commits are pushed and both local jars match the published hash below. Swift `DeviceNode.onShed` only forwards an optional callback, and the current iOS app installs no handler that moves relays, so this Java handoff defect does not require a Swift patch. The iPhone remains on **0.1.26 build 24**. No desktop installer release or fleet deployment was triggered.

## Polish and panel fixes

The shared panel lives in `account/src/main/resources/panel/`; Cloud and Parlons Node package the same assets, and both Electron apps embed that account-owned panel.

- Existing conversation generation checks now also protect older history, send refreshes and delayed upload refreshes. Uploads retain their original recipient and group flag if the user navigates away. Empty/oversized files are rejected; oversized non-converted blobs are rejected before reading them into memory.
- Summary refreshes reuse `renderChatsList()` instead of rebuilding the page/search field. Search replies also carry a generation check, preventing older searches from appending into a newer result list.
- Failed account checks show offline; an expired session remains signed out. Initial account/chat/settings/node failures expose retry actions. Privacy and relay switches keep their old value on failure, disable while saving, and retain their button reference across asynchronous work.
- Clickable conversation/contact rows work from the keyboard. Icon controls have names, switches expose their checked state, sheets have a close button, Escape handling and focus containment, and form focus is visible. Fonts can display while loading, narrow layouts wrap safely, and reduced-motion preference disables switch animations.
- Secondary text uses the same `#676C73` light colour across Android, portal, Swing, iOS and the panel. It exceeds 4.5:1 on white, `#F5F4F1` and `#EFEEEB`. Existing dark palettes and Manrope styling were retained. This claim is limited to this text token; it is not a full accessibility certification.
- iOS `App/Sources/Settings/SettingsView.swift` now decodes pending device keys as the string array actually returned by `ParlonsControl`. Profile/privacy/device failures are visible. Merely loading read-receipt settings no longer writes them back; unavailable privacy settings do not masquerade as a known value. Contact rename/removal only report success after the RPC succeeds. Copy feedback resets when its value changes.
- At the owner's request, iOS `App/Sources/Chats/ChatsListView.swift` displays the installed app version at the top left, opposite the relay/connection status. It reuses the bundle-version lookup from `support/freezepeach-ios/App/Sources/Settings.swift`, so future builds update the label automatically. The label uses existing typography/colours and a spoken accessibility label. **0.1.19 build 17** passed the signed device build and strict signature verification, was installed and launched on the connected iPhone, and the device confirms that version. Commit: `39a2ad09c0751d68e6aed2c526255038971dec27`. This iOS-only presentation change requires no Java or desktop dependency change.
- minimaDesk's `renderer/src/shell/views/ParlonsView.tsx` explicitly restarts sign-in on Reload, bounds ticket retries, clears retry timers and obtains a fresh link after a status failure unmounts the panel.
- minimaCore Desktop's `main/node-manager.js` exposes its existing exact start time, following minimaDesk. `renderer/app.js` uses that stable session key, rejects stale render replies and exposes retry/error states instead of blank or endless spinners.

Eight panel regressions and five desktop session regressions pass. Seven panel cases and all five wrapper cases also demonstrate failures against the original code; the switch helper has separate success/failure coverage. The session tests run the shipped source with deterministic UI/IPC boundaries; they do not replace end-to-end Electron tests.

Safari inspection used the isolated fake-data preview: chat list, conversation, contact sheet and Escape dismissal, settings, dark palette, and the single-column breakpoint at increased browser zoom. No real account was connected. Native Android/iOS/Swing visual validation remains outstanding; iPhone installation and launch are recorded above.

## Continuation: bounded relay-advisory admission

`core/src/main/java/com/eurobuddha/maxima/core/session/HostPool.java` previously put every remote load-shedding advisory on an unbounded single-worker executor, even when the connection had retired, was preferred, had recently honoured advice, or already had work queued. A noisy attached relay could retain arbitrary queued work while an alternative dial was slow.

Commit `4a669a0e13809f89b85ca2e64c0ba1c985745feb` reuses the bounded executor pattern from `wakeproxy/.../WakeHandler.java` and locked admission/rollback from `core/.../util/SerialLanes.java`. One worker admits at most 32 queued jobs. Admission checks the exact active connection, preferred status and existing 30-minute allowance, then coalesces outstanding work per connection. Rejection rolls back ownership; other executor runtime failures also roll it back before propagating. Shutdown clears queued ownership. Network work remains off the socket reader.

Seven `HostShedAdmissionTest` cases cover repetition, independent connections, saturation/retry, preferred/retired/cooldown rejection, shutdown and a failing thread factory. Four initial regressions fail against the prior enqueue algorithm with an explicit unbounded executor substituted only to inspect its queue. Review found an additional rollback defect in the first candidate when thread creation throws; its regression failed before the final repair. Final local evidence: 268 JVM tests, 63 wire vectors and 18 JavaScript tests pass. The Node 0.2.77 release and both desktop copies were byte-identical (`54daa200805ee9b8ddcd53003bd8e615f8d857efd57bc3ca2c8babc85023b60c`). minimaCore 0.16.52 commit `79ef1eca0a789422c2860041d126b79af8ea5d2b` and minimaDesk 0.7.36 commit `611f91489be00a8d6f4a206e61993940bcba5128` were pushed.

Gain: bounded memory/work from advisory floods. Sovereignty: advice is optional and contains no destination; candidate selection remains local and random, and preferred relays stay protected. Under overload advice may be ignored, retaining the current connection; there is no central fallback. In-progress dials, total attached population and historical host maps remain separate review targets.

The [CI run for this commit](https://github.com/eurobuddha/maxima/actions/runs/34403177374) failed in the existing `PeerDiscoveryTest.aDeadVerifiedPeerGetsOneRecheckInThirtyMinutesThenGoes`, not an advisory test. Investigation and its separate versioned repair follow.

## Continuation: TCP self-connections could falsely verify a dead relay

The CI assertion expected a closed relay to leave the verified set, but it remained. In an isolated Linux container, with no external network, narrowing only that container's ephemeral port range to two ports reproduced the exact original test failure. Socket diagnostics showed identical local/remote endpoints: Linux connected the probe to itself, and the existing greeting exchange accepted its own echoed greeting. A separate prebound real-socket regression reproduced this deterministically. The original CI report has no socket telemetry, so its individual connection cannot be reconstructed; the same failure mechanism and assertion are now demonstrated locally.

`core/src/main/java/com/eurobuddha/maxima/core/net/Probe.java` keeps its existing dial alias, connection/read budgets, bounded frame reader, greeting format and socket cleanup. Immediately after connect, it rejects equal local/remote socket addresses before writing a greeting. A package-private supplied-socket entry point permits deterministic source binding in tests; public callers still create their own socket. `ProbeConnectionTest` reuses the live `DirectEndpoint` donor from `core/src/test/java/com/eurobuddha/maxima/core/ProbeTest.java`: self-connected sockets reject and close; ordinary loopback and logical aliases still reach separate endpoints. The regression fails against the old algorithm with only the supplied-socket seam added, then all three cases pass in isolated Linux. The exact two-port discovery reproduction also passes with the repaired production classes. macOS refuses self-connect at connect time; its passing test covers that refusal/cleanup, while Linux exercises the new guard.

Commit `3221d019763fd422a7ffde31fad25b849caa86bc` bumps all affected Java deliverables. Final local checks pass: 271 JVM tests, 63 wire vectors, 18 JavaScript regressions, and affected Android/portal/Node/Cloud/server/Swing builds. Node, Cloud and server jars contain the exact tested `Probe.class`; all 11 panel assets in Node and Cloud match source. Android metadata confirms 0.6.94/code 694 and portal 0.2.41/code 85. The iPhone remains on 0.1.26/build 24; the Swift implementation has no equivalent Java TCP discovery probe, and no new iPhone install was needed.

Gain: a dead local/aliased endpoint no longer receives false reachability credit from its own greeting. Sovereignty: verification still happens independently on each client or relay, with no registry, privileged operator or new dependency. This is required local transport validation, not an optional hosted service. It does not authenticate a hostile remote greeting or claim to repair every transport's self-connection handling. A macOS stress run stopped at iteration 475 on an initial-greeting failure before instrumentation; subsequent 2,000-iteration macOS and Linux diagnostic runs passed. That separate initial-greeting failure was not used to justify this repair.

The repaired commit passed [Linux CI](https://github.com/eurobuddha/maxima/actions/runs/34405242937). Review approved the endpoint guard and existing cleanup/alias semantics; the regression uses a real Linux socket rather than a mocked endpoint comparison. minimaCore 0.16.53 commit `0eb4e63a9da244874b449bacf56ba0f41977a877` and minimaDesk 0.7.37 commit `583bdccabd36e9a134cbfc0682fd0177a1454680` are pushed and pin Node 0.2.78. At that step both fetched jars matched the Node 0.2.78 release (`b98f31f66e5b3011f442189e845e173e3d989e20904ca32db7ef8cbb0fd055cd`). minimaCore's unrelated pending source and test script remain uncommitted, with its pending manifest version advanced to 0.16.54. No desktop installer or fleet release occurred.

## Continuation: ownership and cancellation for peer-discovery probes

**Major finding:** `core/src/main/java/com/eurobuddha/maxima/core/session/PeerDiscovery.java` did not retire queued or running probes when `noConnect`, `addSelf` or `stop` invalidated their work. A greeting that finished afterward could re-adopt a removed peer, and a queued peer could still be dialled after removal. Shutdown interrupted the worker but left its socket read and queued work alive. A full queue's failed `offer` also left an unverified peer without a scheduled retry.

Commit `fac59cb7029bf67f2aa0b9aab1880acf32ac60a5` reuses the exact-work ownership pattern in `HostPool.java` and the existing supplied-socket greeting exchange in `Probe.java`. That overload is now public so the discovery owner can close its socket to cancel the operation. Each peer has at most one owned queued/running `Check`; retirement removes the exact check and closes its socket. Shutdown clears pending admission and deferred work, prevents restart/admission, and closes owned sockets. Results recheck ownership under the same monitor before updating state or calling listeners. Network probing remains outside the monitor. Callback-triggered shutdown is rechecked after eviction before adopting a new peer. Repeated rechecks coalesce; queue overflow retains the existing 60-second deferred retry rather than silently losing it.

`PeerDiscoveryLifecycleTest` reuses `FakeRelay` and the gated greeting/cancellation pattern in `HostPoolLifecycleTest`. All eight cases fail against a scratch compilation of the unchanged prior `PeerDiscovery.java`, then pass with the repair: stop/cancel/discard, refusal after stop, in-flight removal, a newly recognised self address, queued removal, full-queue retry, recheck coalescing and a listener stopping during eviction. Review caught the last callback boundary in the first candidate; that test failed before the final ownership recheck. The saturation fixture fills only the private test queue and advances its deferred time; it demonstrates correct overload handling, not a claim that normal traffic commonly fills the queue.

Final local validation: 279 JVM tests (core 174), 63 wire vectors and 18 JavaScript tests, all passing. The 25 lifecycle/discovery/probe cases also pass on isolated Linux. All affected Java builds pass; Node, Cloud and server jars contain the five exact tested discovery/probe classes. Node and Cloud each contain all 11 current panel assets. Android metadata is 0.6.95/code 695 and portal 0.2.42/code 86. The Swift client has no corresponding Java peer-discovery worker, and the standalone wake proxy has no core dependency; iOS stays at 0.1.26/build 24 and wake proxy at 0.1.3.

Gain: obsolete probes release sockets promptly and cannot restore removed peers; repeated work coalesces, and overload remains retryable. Sovereignty: each node still verifies peers independently, uses the same random selection and admission policy, retains user-controlled bootstrap/preference choices, and needs no new service, registry or privileged operator. This is required local lifecycle handling. Custom listeners and restore-time store reads are serialized with discovery mutations; slow custom callbacks can delay those mutations. Closing a socket does not promise cancellation of an operating-system DNS lookup. Persistence snapshot/dirty-state races, loaded-list bounds and separate relay-mesh lifecycle handling remain review targets; they are not claimed solved by this change.

## Continuation: the Java 21 test relay could greet after close

The [CI run for the discovery lifecycle repair](https://github.com/eurobuddha/maxima/actions/runs/34407042302) passed its eight new cases and the self-connection regression, but failed again at the existing dead-relay assertion. A matching Linux x86-64/Temurin 21 container reproduced it at iteration 173. Scratch-only diagnostics showed a new connection being accepted and greeted with `running=false` and `server.isClosed()=true`, after `FakeRelay.close()` had returned. This was a second, distinct cause of the same assertion: discovery correctly recognised an endpoint that the test fixture had failed to stop. The earlier TCP self-connection defect remains independently reproduced and fixed.

Commit `c0a27c6856e01a6adeee3126accae5feb8da0a7e` retains the fixture's accept thread, refuses sockets accepted after stop, serializes greeting publication with close, and waits up to five seconds for the accept loop to finish. Failure to terminate is an explicit test failure. The delayed-greeting regression fails against the old fixture and passes with the repair. The exact formerly failing discovery test passes 2,000 repetitions on Linux x86-64/Java 21, and all 26 focused discovery/probe cases pass on that runtime. The full local JVM gate passes 280 tests (core 175) plus 63 wire vectors. Existing panel/desktop regressions remain 18 passing cases; the five minimaCore integration cases also pass atop the concurrent remote changes.

Node is versioned 0.2.80 to follow the per-code-change rule. Bytecode comparison with the 0.2.79 artifact confirms that only `ParlonsNodeMain.class`, containing the version constant, changed; production discovery/probe classes are identical. Other Java deliverables retain their 0.2.79-pass versions because this additional change affects the test fixture, not their shipped code. The correction improves test evidence without changing network policy, protocol or decentralisation.

minimaCore's remote advanced concurrently to `93e8c21b9589d76dcf5ea6f5d1022c2d208a4e00`. The dependency update was integrated on top in an isolated checkout, then pushed normally as 0.16.55 commit `bf5cb52335f3ae4ef818f8759004039fd26b60f6`. The main checkout now uses that base; 23 pending files were checked byte-for-byte and preserved, the newly introduced review file was brought in, and pending manifest metadata is 0.16.56. The superseded unpublished dependency commit is retained on `codex/parlons-core-pre-integration-0.2.79`. minimaDesk 0.7.39 commit `9a725ad7ca8b80a60b37a5e250a83e7aa446a6d1` is also pushed. Both pin the final Node 0.2.80 artifact. No desktop installer or fleet deployment occurred.

The final fixture-corrected commit passed [Linux CI](https://github.com/eurobuddha/maxima/actions/runs/34408195287), including the full JVM and panel steps. Both main desktop checkout jars are byte-identical to the final release checksum below.

## Continuation: explicit storage failures and retryable snapshots

`FileStore.writeAtomic` logged failed writes and returned success; `flush` cleared every dirty collection before attempting persistence. A real filesystem obstruction reproduced a missing record after storage recovered and a second flush returned: `/private/tmp/parlons-store-failure-baseline.log` (`failureReported=false`, `retriedRecord=null`). Immediate writes also had no retained retry marker. The rename fallback could delete an unexpected target to force replacement.

`core/src/main/java/com/eurobuddha/maxima/core/store/FileStore.java` now reuses the private temporary-file, fsync and replacement sequence from `account/src/main/java/com/eurobuddha/maxima/cloud/AccountFiles.java`. It preserves TSV/log escaping and streamed writes. Write/replacement failures throw `UncheckedIOException`; dirty collections are removed only after successful persistence, including failed immediate put/remove operations. Replacement does not explicitly unlink the previous target. Temporary files are private from creation on POSIX systems and are cleaned up. The existing fallback remains limited to `AtomicMoveNotSupportedException`.

The caller review found that `ChatEngine.flushState` skipped buffered new messages when no deferred receipt/state changes existed, and discarded the rest of a state batch after a failed immediate write. It now always flushes the store and requeues the failed batch from current entries. `ChatEngine.close` and `MaximaNode.stop` release their existing worker pools in `finally` even when persistence throws. Retry remains possible after the disk obstruction is removed; no identity or storage schema changes are involved.

Ten new regressions cover failed writes/removals, multi-collection retry, unexpected directory targets, log escaping, private file modes, chat state retry, buffered-message shutdown and worker cleanup. All ten fail against the previous production classes (`/private/tmp/parlons-store-tests-baseline.log`). A second baseline combines the repaired store with the old callers to isolate their failures (`/private/tmp/parlons-store-callers-baseline.log`). All 12 focused cases, including the two existing coalescing tests, pass locally and in an isolated Linux amd64 Java 21 container with no network. The full family run passes 290 JVM tests, 63 wire vectors and both Android release builds (`/private/tmp/parlons-store-family.log`).

Code review: approved for the explicit storage-error/retry scope after adding caller cleanup. The gain is recovery from transient disk errors and fewer silently lost snapshots. Account owners retain their local stores and independent relays; there is no new service, registry, trust authority or mandatory network participant. The store implementation remains host-replaceable. Atomic-move fallback power-loss behavior, binary-record replacement, append/read error handling and cross-process writers are not certified by these tests.

**Delivery issue identified during the storage pass (repaired below):** `HostConnection.answerMailboxChallenge` catches a failed pre-ack flush and still signs the deletion acknowledgement. `MaximaNode.runFlushHooks` also swallows store/hook errors, and `drainInbound` ignores a timed-out wait. `RelayServer.handleMailboxAck` calls `Mailbox.acknowledge`, which deletes the acknowledged records; the client comment claiming TTL retention after an acknowledgement is incorrect. The storage repair alone did not close that issue; the subsequent mailbox-gate continuation below covers these error paths. The Swift pre-ack callback and asynchronous delivery ordering also need corresponding review.

Storage repair commit: `0e736c2e0ca784cb16ab38fbfccbbe8dea1dac1e`; its [exact-commit Linux CI run](https://github.com/eurobuddha/maxima/actions/runs/34410303048) passed the full JVM and panel suites. Desktop propagation commits are minimaCore `581a1ea38361e6d8855abf6341d50f783adb501f` and minimaDesk `cf732640cdfddc387831d54cfd99936eb8649e9e`. Both existing checksum fetch scripts accepted the same Node 0.2.81 artifact. All ten desktop HTTP/session tests pass; minimaDesk’s initial HTTP run was denied loopback bindings by the sandbox and passed unchanged with the necessary local socket permission (`/private/tmp/parlons-store-desk-rpc-final.log`). Eight unrelated tracked pending minimaCore files were checked byte-for-byte after propagation; only its version metadata advanced for the new pending work. No installed desktop app or fleet node was launched or updated.

## Continuation: mailbox deletion requires a successful flush gate

The previous client signed deletion acknowledgements after failed flush hooks. The node suppressed both its own store errors and application-hook errors, ignored inbound wait timeouts/interruption, and treated a queue-full `CallerRunsPolicy` barrier as if it had waited behind the worker. This was reproduced against an actual isolated relay: a message disappeared from its mailbox while the client's temporary store could not persist it.

`HostConnection.answerMailboxChallenge` now validates the routing key and nonnegative sequence before any hook. Sequence zero remains the existing signed ownership proof and needs no flush because it deletes no mail. Positive sequences run the pre-ack hook before signing; exceptions or interruption withhold deletion permission. The domain tag, key, sequence and signature encoding are unchanged.

`MaximaNode.runFlushHooks` now requires an explicitly successful inbound barrier and propagates store/application flush errors. `drainInbound` reuses the bounded `CompletableFuture.get` pattern from `HostConnection.send`; it returns false for timeouts, interruption, shutdown, execution inside the current delivery, and a barrier executed inline by the queue-full fallback. It preserves interruption and removes expired queued barriers. The existing bounded queues, 10-second ack wait and relay retry cadence remain intact.

Twelve new regressions cover these gates, cancellation, direct capture of absent ACK bytes, verification of the unchanged signed sequence-zero proof, and two real-relay disk-failure/recovery cases. In both relay cases the copy remains while node/application storage fails, then the normal periodic drain retries and clears it only after a fresh FileStore sees the saved message. Existing dedup keeps the application delivery count at one. All twelve fail against the previous production classes (`/private/tmp/parlons-ack-baseline-final.log`) and pass on macOS and isolated Linux amd64 Java 21 (`/private/tmp/parlons-ack-linux-check.log`). The full family run passes 302 JVM tests, 63 wire vectors and both Android release builds (`/private/tmp/parlons-ack-family.log`).

Code review: approved for the failed-flush and incomplete-barrier paths. Gain: a local disk error or stalled worker no longer authorizes deleting the only relay-held copy through these gates. Sovereignty: each owner still chooses their stores and independent relays; no registry, authority or central recovery service is introduced. The optional store/flush integration remains host-supplied, and classic clients retain their existing TTL semantics. Risk: an unavailable store delays acknowledgement until recovery or relay expiry; existing bounded waits and periodic re-drain provide the retry path.

Limits requiring further review: exceptions inside application delivery before any store write, concurrent duplicate deliveries across connections, RPC-lane completion, and mailbox TTL versus client freshness policy. This pass proves the keyed-store error gates and the sequential-listener fixture, not all application delivery semantics. In the Swift variant, `HostConnection.beforeAck` and `HostPool.beforeAck` are synchronous nonthrowing callbacks, but no production application caller currently installs one; a mechanical Java port would not establish cache/RPC completion. Swift delivery/cache ordering remains a separate open review, and the installed iPhone build is unchanged.

Mailbox-gate source commit: `dd7ab68667bbdd1f3438853da60778c59b2215df`; the [exact-commit Linux CI run](https://github.com/eurobuddha/maxima/actions/runs/34411794983) passed the complete JVM and panel suites. Propagation commits: minimaCore `f5e68deeff0305bb17cadc3c40db3a84b4791fc4`, minimaDesk `ebd029d17eebc510af2871f68dfe201eac1e315b`. Both existing fetch scripts verified the Node 0.2.82 release checksum, and both local jars match it byte-for-byte. All ten existing desktop RPC/session tests pass. Eight unrelated tracked pending minimaCore files remain byte-identical, with pending version metadata advanced separately to 0.16.58. No fleet or desktop installer deployment occurred.

## Continuation: cleared and pruned messages stay removed across restarts

A synthetic FileStore replay reproduced cleared content returning after restart: `initial=1`, `afterClear=0`, `afterRestart=0`, `afterExactReplay=1` (`/private/tmp/parlons-clear-replay-evidence.log`). The existing 4,000-ID removal ring was only in memory, and clear did not populate it. A deferred state write could also write a captured entry after a concurrent clear.

`ChatEngine` now reuses that bounded ring and the existing atomic `chat_messages` collection snapshot. Removal replaces the payload at its existing key with an ID/order marker; it retains no message body. Load restores markers in removal order, rather than the original message insertion order. Clear and deferred state writes now share the existing chat monitor with record/prune. A failed eviction cleanup can leave a live entry, so retry always rewrites its removal marker even if the in-memory ring already contains the ID. No forced per-message flush or additional collection was introduced. The original message JSON format is unchanged. An actual dataset written by the new engine was loaded by the prior production reader: it skipped the no-message-id markers and displayed zero bubbles (`/private/tmp/parlons-removal-old-reader.log`); this does not give older readers the new replay protection.

Five regressions cover clear/replay before and after restart, prune/replay, the 4,000-marker budget and removal order, failed-eviction retry after a deferred state save, and concurrent state-write/clear. All five fail against the previous production engine (`/private/tmp/parlons-removal-baseline-final.log`). The failed-eviction case also fails against the initial candidate (`/private/tmp/parlons-removal-review-baseline.log`), and all five pass with the final implementation. Final full-family validation passes **307 JVM tests**, **63 wire vectors**, both Android release builds and Node/Cloud/relay fat jars (`/private/tmp/parlons-removal-family-final.log`). The three jars contain the exact final compiled ChatEngine class. The initial four removal tests and existing four chat persistence/receipt tests also passed on isolated Linux amd64 Java 21; final Linux CI is checked separately. Automatic approval review timed out on earlier Gradle launches; the final full-family retry was accepted and completed successfully.

Code review: the change addresses the reproduced removal/replay and stale-state-write paths. Gain: removed payloads stay removed through normal reopen/replay within the existing bounded history, without retaining their content. Sovereignty: this is entirely local owner-controlled storage; it adds no authority, registry, relay requirement or recovery service. Risk: clearing and state flush now serialize disk work under the existing chat monitor. Coalesced storage remains coalesced, and the marker budget remains 4,000. This is bounded replay memory, not permanent unlimited deletion history. Corrupt input, interrupted cleanup, disk failure and power-loss semantics remain subject to the store's existing durability limits.

A separate real-loopback reproduction confirmed an outstanding age-policy defect: the relay retains mail for seven days, while `MaximaNode.handle` applies the six-hour DedupCache freshness window to held mail too. The fixture reports `ageHours=0 delivered=1 relayCopies=0` and `ageHours=24 delivered=0 relayCopies=0` (`/private/tmp/parlons-age-evidence.log`): the old item is dropped and its relay copy acknowledged away. This pass deliberately leaves that policy unchanged. A repair must distinguish delayed conversation content from command, roster, wallet-address and call replay; blindly widening the window would weaken existing security checks. The removal repair above is a prerequisite, not a claimed fix for aged mail.

Removal-persistence source commit: `6335327a0c05a65ec81166e24541a80c9e213a1e`; its [exact-commit Linux CI run](https://github.com/eurobuddha/maxima/actions/runs/34449148673) passed the complete JVM and panel suites. Propagation commits: minimaCore `90f5c43071c972d6880f4fbc30cad8d0a0d32d2f`, minimaDesk `611c0757acb4db2326a7fb91a7600f6d655c2359`. Both existing fetch scripts accepted Node 0.2.83 and both local jars match the release checksum. All ten desktop RPC/session tests pass. Eight unrelated tracked minimaCore files remain byte-identical; its pending working-tree version is now 0.16.59. No scratch replay fixture or removal-test container remained running. Swift/iPhone code is unchanged in this pass; no fleet or desktop installer deployment occurred.

## Continuation: delayed conversation content within mailbox retention

The age-policy reproduction was narrowed to actual supported chat payloads using the existing `MailboxDurabilityTest`/`AttachedSendTest` loopback fixtures, real signed/encrypted MaxTxPoW carriers, synthetic identities and temporary FileStores. Against the previous production code, three of the initial five tests fail: day-old text is absent after relay deletion, six-day-old conversation content is absent, and old-message redelivery never reaches chat. The negative controls already pass (`/private/tmp/parlons-age-baseline.log`).

`MaximaNode.handle` keeps the default six-hour DedupCache check. Only when it returns STALE does the node inspect a narrowly recognised history payload within seven days of its signed transport timestamp: native text/group text/payment notifications with a nonempty stable ID, or a parseable classic MaxSolo content object. It reuses `DedupCache.seenBefore` and its existing bounded 20,000-entry cache. Native decoding uses the same `ChatMessage.decode` as ChatEngine; classic decoding uses the same `ClassicChat.parse`. The original wire format and signatures are unchanged. Future dates gain no extra allowance. RPC, calls, roster changes, wallet addresses, receipts, unknown applications and malformed history retain the original gate. Newly admitted old content does not refresh the sender's online/last-seen indicator.

Six real-relay regressions now pass: durable day-old text before mailbox deletion, six-day text/group/payment/classic text/image content, rejection of old controls and malformed/unknown payloads while a fresh RPC still executes, rejection of future/beyond-retention/extreme timestamps, redelivery dedup, and enforcement of current group membership after a sender has been removed. The payment fixture is a display notification only; no wallet operation occurs. The full family gate passes 312 JVM tests, 63 vectors, both Android release builds, the standalone desktop jar and Node/Cloud/relay fat jars (`/private/tmp/parlons-age-family.log`). The added membership case and final complete 45-test relay run pass (`/private/tmp/parlons-age-server-final.log`), bringing the verified family set to **313 tests**. The existing standalone six-hour freshness/dedup/outbox suite also passes all 12 checks (`/private/tmp/parlons-age-reliability.log`). Both MaximaNode and ChatEngine classes inside all three fat jars match the final build outputs.

Code review: approved for delayed display content within the stated signed-time horizon. Gain: supported conversation content no longer disappears solely because it spent more than six hours awaiting delivery. Sovereignty: admission remains a local receiver policy; it trusts no relay claim of storage time and adds no registry, preferred operator, central recovery path or mandatory service. The existing independent relay choice and owner-controlled bootstrap remain unchanged. This is a default local compatibility policy, replaceable by independently built clients without a new wire requirement. Risk: signed conversation content is replay-admissible for a longer period. Existing transport dedup, ChatEngine message IDs, persisted bounded removal history and current group authorization remain in force; tests exercise redelivery and a removed member.

Limits: the wire does not distinguish a live unit from a held one, so the receiver uses signed send time, not the relay's storage timestamp. Deliberate clock skew, unusually delayed insertion and custom longer relay retention can exceed that horizon. Controls still expire after six hours; reliable delivery of an old group invitation/update requires a separately versioned anti-rollback policy, not blanket acceptance of old rosters. Bounded caches cannot promise permanent replay suppression after arbitrary churn. This pass does not fix swallowed application-handler exceptions, cross-connection in-flight duplicates, or RPC completion durability. Swift's inspected HostConnection has no equivalent six-hour filter; the iPhone receives account history through its existing RPC/cache path and needs no mechanical copy of this Java-only admission fix.

Delayed-history source commit: `3e3b9e0c6bd1fd4562480eec2b8b2fc736a88959`; its [exact-commit Linux CI run](https://github.com/eurobuddha/maxima/actions/runs/34450265928) passed the complete JVM and panel suites, including the final membership test. Propagation commits: minimaCore `134c9ac121dbd11da4fc59045100343f1633377c`, minimaDesk `1f87a5c34cf4021ca2108d927935691c41d542a3`. Both existing fetch scripts verified Node 0.2.84; both local jars match its checksum byte-for-byte. All ten desktop RPC/session tests pass. Eight unrelated tracked minimaCore files remain byte-identical, with its pending working-tree version advanced separately to 0.16.60. All source commits are pushed. No installed desktop, fleet or phone deployment occurred in this continuation.

## Continuation: three remaining delivery-completion gaps reproduced

At source commit `3e3b9e0c6bd1fd4562480eec2b8b2fc736a88959` (Node 0.2.84), the following previously open paths are now confirmed. These are findings, not repaired behavior in this continuation.

1. **Application failure before a write still permits deletion.** Reusing the loopback relay setup from `MailboxDurabilityTest`, the synthetic listener increments an attempt counter and throws before storing anything. The positive-sequence flush hook runs and the relay clears its copy: `RESULT handlerThrowsBeforeWrite ageHours=0 delivered=1 relayCopies=0`. Here `delivered` counts listener invocations, not successful delivery. Source: `/private/tmp/parlons-age-evidence/MailboxHandlerEvidence.java`; output: `/private/tmp/parlons-handler-evidence.log`. The node catches the listener exception, and production account/Swing listeners additionally swallow ChatEngine exceptions before the node can observe them.
2. **The RPC lane is outside the acknowledgement barrier.** A signed synthetic request invokes a registered handler that waits on a latch before completing. While that handler is still blocked, the relay clears the request: `RESULT blockedRpc ageHours=0 delivered=0 relayCopies=0`. Source: `/private/tmp/parlons-age-evidence/MailboxRpcEvidence.java`; output: `/private/tmp/parlons-rpc-evidence.log`. `runFlushHooks` drains `mInboundExec`, while RPC runs on `mRpcExec`. The fixture supplies no reply addresses and performs no wallet operation.
3. **A queue-full delivery can outlive a successful worker barrier.** With the single inbound worker blocked and its bounded queue full, `CallerRunsPolicy` runs the next signed/verified delivery on the submitting reader thread. After the worker is released and its queue empties, `drainInbound` returns true while that reader-thread handler is still blocked: `RESULT drainReportedSuccess=true applicationStillRunning=true`. Source: `/private/tmp/parlons-age-evidence/InlineDeliveryEvidence.java`; output: `/private/tmp/parlons-inline-delivery-evidence.log`. This fixture proves the barrier's incomplete scope; it does not claim a reproduced multi-relay loss. The earlier fix correctly rejects an inline *barrier*, but that does not account for an inline *delivery* still running elsewhere.

The reuse review inspected `MaximaNode.handle`, `drainInbound`, `runFlushHooks`, `HostConnection.receive/startReader/answerMailboxChallenge`, `HostPool` hook wiring, `DirectEndpoint`, `RpcPeer.onInbound`, `ServiceRegistry`, the account/Android/Swing listeners, `ChatEngine.record/flushState`, `SerialLanes` and its tests, and the Swift connection callback surface. Existing bounded `CompletableFuture` waits in HostConnection are a useful primitive. SerialLanes alone is not a completion certificate: its drain deliberately catches task failures, and its pending task queues are not an appropriate replacement for the node's bounded lanes. The signed mailbox acknowledgement format and sequence-zero possession proof must remain unchanged.

Repair requirements: associate acknowledgements with completion of the deliveries actually preceding that connection's challenge; account for RPC and inline work; prevent a duplicate from certifying an unfinished original; surface application persistence errors; keep queues/completion state bounded and release it on shutdown; preserve default synchronous consumers and direct-path compatibility; verify recovery against a real relay and temporary persistent stores. Do not infer successful processing from merely running a queue barrier. Do not transparently rerun arbitrary partially executed RPC commands without establishing their idempotency and outcome semantics. Chat payload persistence, command execution, and reply delivery are distinct completion stages. No central coordinator or authority is needed for these local completion rules.

All three fixtures use fresh synthetic identities, bounded waits and finally-block cleanup. This continuation changed only the audit record; production delivery completion remains an open repair.

## Continuation: mailbox acknowledgements follow delivery completion

The three reproduced completion gaps above are repaired in the JVM transport. `MaximaNode` now associates admitted work with a completion future before it can be queued or run inline. Each connection registers that future before returning from its inbound callback. In-flight duplicates share the original future, including after ordinary dedup-cache eviction. A separately bounded active map prevents unfinished work from being mistaken for a completed cached message. The existing 20,000-entry DedupCache also retains completed outcomes only for the lifetime of their dedup entries.

A failed non-RPC application handler removes its dedup admission so the held item can be retried. The account and Swing listeners now propagate ChatEngine errors instead of swallowing them. An unexpected failure outside normal RPC dispatch retains a failed outcome in the bounded cache: identical transport replay cannot immediately repeat an uncertain side effect. Ordinary service exceptions remain normal RPC ERROR results under the existing ServiceRegistry contract. A refused executor submission is distinguished from work that actually started. Shutdown fails outstanding completions, and closed lanes now reject explicitly rather than silently discarding work through CallerRunsPolicy.

`HostConnection` waits for the futures preceding that connection's positive-sequence challenge, then runs the existing node/application flush hooks before signing. The global inbound-worker barrier is no longer the acknowledgement certificate; it remains a best-effort shutdown helper. Each connection keeps at most 4,096 distinct pending futures, and repeated references to one active delivery do not grow that set. Failure/cancellation closes the attachment with the relay copy retained; normal reconnect can recover application delivery. Future state, reader waits and owned acknowledgement work are released/cancelled on close.

Review caught a further defect in the initial candidate: waiting on the reader thread could prevent that same connection from collecting ordinary outbound-send ACKs while an RPC was blocked. A real-relay regression failed against that candidate (`/private/tmp/parlons-completion-reader-baseline.log`). Positive mailbox acknowledgements now run on a shared, bounded daemon executor, reusing RpcPeer's pool pattern with **at most 8 workers and 256 queued tasks**, an abort-on-saturation policy and idle thread expiry. Each connection owns one task and coalesces newer challenges into one bounded snapshot. Closing cancels/removes that task. Saturation produces no deletion permission; the existing periodic relay drain retries after capacity recovers. Sequence-zero possession proof remains immediate. The signed key/sequence format and wire vectors are unchanged.

Fourteen new regressions cover listener failure/recovery, actual ChatEngine disk-failure recovery, RPC completion before flush/deletion, duplicate ownership through cache churn/reconnect, inline delivery, uncertain RPC outcome suppression, synchronous consumer failure, shutdown, closed-lane rejection, per-connection state bounds, cached-outcome eviction/forget/clear, concurrent send-ACK progress, challenge coalescing/cancellation, and acknowledgement-pool saturation/recovery. The six selected delivery-behavior cases all fail against the prior production classes (`/private/tmp/parlons-completion-baseline.log`). The final complete family gate passes **327 JVM tests**, **63 vectors**, both Android release builds, the standalone desktop jar and Node/Cloud/relay fat jars (`/private/tmp/parlons-completion-family-final.log`). Final core classes inside all three fat jars match the compiled outputs. Existing ownership-signature, failed-flush and interruption tests also pass; the interruption fixture now observes the acknowledgement worker that owns the flush.

Code review: approved for the reproduced callback-completion, duplicate, inline-work and blocked-RPC acknowledgement paths, with the limits below. Gain: a connection cannot certify unfinished/failed tracked delivery merely because another worker queue drained, and waiting for delivery no longer deafens its reader. Sovereignty: all state and work remain local to the owner's node; independently selected relays retain the unchanged signed ownership protocol. No coordinator, authority, registry, new relay capability or mandatory external service is introduced. Synchronous raw consumers retain their existing contract; asynchronous embedders can supply completion through the local Inbound API. Risk: handler/storage errors or worker saturation retain mail and can require a reconnect or periodic re-drain. State/work limits, cancellation, timeout and real-relay recovery tests cover those gates.

Limits: completion means the registered callback/dispatcher returned and its installed stores flushed. It does not prove delivery of an RPC reply, completion of a separately queued console/wallet job, or persistence inside a different Android IPC process. Dedup/outcome suppression is bounded and in-memory, not a durable exactly-once command ledger across restart or arbitrary churn. Current membership and the six-hour control/seven-day history policies remain unchanged. CallerRuns still permits concurrent application execution under saturation; this repair tracks its completion, not all application ordering. Swift has separate nonthrowing/async callbacks and still needs its own delivery/cache completion integration. No iPhone install is part of this pass.

Delivery-completion source commit: `1f16a55dd577929ee1ec93de69568444fa8e2810`; its [exact-commit Linux CI run](https://github.com/eurobuddha/maxima/actions/runs/34454058654) passed the complete JVM and panel suites. Propagation commits: minimaCore `0ac15f79fd3d04ec39ae9454645890363c5bdc9f`, minimaDesk `fca541c8ca4548d8d0923546adcb796dcf969615`. Both existing fetch scripts verified Node 0.2.85 and both local jars match its checksum. All ten desktop RPC/session tests pass. All 13 compiled class files in the three changed core class families were compared with each Node/Cloud/relay fat jar. Eight unrelated tracked minimaCore files remain byte-identical; its pending working-tree version is 0.16.61. Source commits are pushed. No installer, fleet or iPhone deployment occurred in this continuation.

## Propagation and versions

| Variant | Resulting source version | Propagation |
| --- | --- | --- |
| Android Parlons | 0.6.119, code 719 | Shared core fixes; native contrast; release APK built |
| Android Cloud portal | 0.2.66, code 110 | Shared core fixes; native contrast; release APK built |
| Standalone Swing desktop | 1.5.98 | Shared core and file-counter fixes; native contrast; jar built |
| Parlons Cloud | 0.11.101 | Shared core/account/counter/panel fixes; fat jar built |
| Parlons Node | 0.2.104 | Shared core/account/panel fixes and admin RPC protection; dependency release published |
| Relay server | 0.4.99 | Shared directory/RPC/receipt fixes; fat jar built |
| Optional wake proxy | 0.1.5 | Bounded work/responses and independent token limits; standalone release download verified; not redeployed |
| iOS app and notification extension | 0.1.31, build 29 | Ordered writes, attachment lifecycle/cancellation, approvals/settings feedback, shared contrast, visible version label, foreground/RPC/resolver lifecycle ownership, shared relay preferences, notification/background completion, full catch-up deadline, cached refresh ownership, atomic snapshots, mailbox challenge validation, safe timestamp admission, bounded delivery-completion tracking, ordered-send ACK admission and overflow-safe RPC waits; installed and launched on iPhone |
| minimaCore Desktop | 0.16.78 | Node 0.2.104 pin and fetched jar; RPC and panel-session fixes; separate pending working-tree version 0.16.79 |
| minimaDesk | 0.7.62 | Node 0.2.104 pin and fetched jar; RPC and panel-session fixes |

Both Electron `resources/parlons-node.jar` files are byte-identical to the checksum-verified release:

`aa66cc38c78d68434faa4753a31251d5b2c66709bd67ba46e4e1b531d76ba71a`

Node dependency release: <https://github.com/eurobuddha/maxima/releases/tag/node-v0.2.104>.
The panel assets inside the Node and Cloud fat jars were compared byte-for-byte with source. The underlying `core/minima-core` fork needed no source change for these repairs; Node bundles the existing Minima runtime together with the repaired Parlons modules. Classic `maxjar` remains on its separate classpath and its compatibility check passed.

All authored code changes are committed and pushed in their respective repositories. Existing unrelated working changes and generated graph/dist files were preserved. Source propagation and locally prepared dependencies are complete; this audit did **not** publish new desktop installers, update installed desktop apps, publicly distribute iOS builds, install phone APKs or redeploy the relay fleet. The connected iPhone received the authorized development build described above. Desktop distribution still requires the normal three-platform packaging and signed/notarized Mac release process.

### Swift mailbox challenge validation — 0.1.27 build 25

The Swift `HostConnection.answerMailboxChallenge` called its flush hook before parsing or validating a challenge, converted an out-of-range/fractional `MiniNumber` to sequence zero, and accepted negative sequences. Its nonthrowing hook could not report failure. The shared JVM path already had the key/nonnegative-sequence and ownership/flush separation safeguards, propagated into Node 0.2.85 and both desktop dependencies.

Reused `maxima/core/src/main/java/com/eurobuddha/maxima/core/net/HostConnection.java` and `maxima/server/src/junit/java/com/eurobuddha/maxima/server/MailboxDurabilityTest.java`, with the iOS `AttachedSendTests.swift` loopback relay. Swift now requires the existing exact `MiniNumber.int64Value` conversion and matching per-host key before side effects. Zero proves ownership without flushing; positive sequences flush before signing. `HostConnection.beforeAck` and `HostPool.beforeAck` accept throwing hooks; a thrown error withholds deletion permission. Cancellation is checked before validation and after the hook. Canonical signature bytes and wire encoding are unchanged.

Eight wire-level regressions cover successful flush and signed Int64.max ACK, failed flush, ownership proof with unavailable storage, wrong routing key, negative/overflowing/fractional sequence and truncated input. Six of the seven initially added cases failed against prior production code (seven assertions); the healthy positive case passed. All eight pass with the fix, alongside the full **73-test Swift package suite** and **36-test simulator app suite**. The app and notification extension build as **0.1.27 build 25** for the physical iPhone. Installation and launch succeeded, and normal device app metadata reports `com.eurobuddha.parlons` version 0.1.27 build 25. Strict deep signature verification passes with normal macOS trust-service access; the sandbox-only verification could not access that trust context. Code commit: `a40cfae2e419cb44f96a5a2c162aaecba5762fa3`, pushed to iOS master.

Review: approved for this narrow challenge gate. There is no production iOS caller installing `beforeAck`; the app is a paired-device client whose account retains history, and its cache is a refreshable snapshot. This repair does **not** establish application delivery completion: `RpcPeer` starts asynchronous dispatch, `PushService` invokes a nonthrowing sink, and `SessionController` schedules MainActor work. Positive mailbox ACK tasks are also still independently scheduled. Bounded ownership, asynchronous completion and cache/cursor coordination remain open review areas. No claim of phone message loss or durable exactly-once RPC processing follows from these tests.

Sovereignty: local validation gains stricter deletion authorization without changing operator choice, owner-controlled bootstrap, identity custody or interoperability for valid challenges. This is a client correctness repair, with no required external component or new authority. Risk: invalid challenges receive no ACK; valid zero/positive challenge tests and the wire parity suite mitigate accidental rejection of normal relay traffic. Desktop JVM code did not change in this pass because it already supplies the donor behavior; both wrappers retain their verified Node 0.2.85 dependency.

### High: inbound timestamps could crash Swift or bypass JVM freshness validation

The Swift `ParlonsKit/Sources/ParlonsKit/Session/DeviceNode.swift` computed `abs(nowMs() - sent)` on an untrusted signed timestamp. A synthetic call through the real inbound handler with Int64.min terminated XCTest with signal 5. Separately, rejected stale messages entered the 2,048-entry dedup cache first: a regression demonstrated that 2,048 stale inputs evicted an accepted message and allowed it to dispatch again. These are handler-level reproductions with synthetic identities, not a reported delivery incident on the owner's phone.

The shared JVM `core/src/main/java/com/eurobuddha/maxima/core/reliability/DedupCache.java` used the same absolute-difference pattern. Java wraps instead of trapping: abs(Long.MIN_VALUE) stays negative and passes the freshness bound. Two new baseline tests failed: a timestamp farther than Long.MAX_VALUE even with the largest configurable window, and an actual default-window bypass sampled in the same clock millisecond. `MaximaNode.handle` also narrowed MiniNumber with `longValue()`: fractional time or a negative value offset by 2^64 could become a fresh timestamp. A separate handler regression failed before repair and now round-trips its malformed values through the real wire codec before checking admission.

Reuse review covered Swift DeviceNode, MiniNumber, RpcPeer and RpcLifecycleTests; JVM DedupCache, ReliabilityUnitTest, DedupCompletionTest, MaximaNode's retained-chat branch and MailboxAgeTest; classic `maxjar/src/main/java/org/minima/system/network/maxima/MaximaManager.java` and its message codec; and the sibling freezepeach-ios sources. The existing admission paths are retained. Classic has no freshness gate to reuse, and no compatible sibling Swift arithmetic helper exists. Swift uses its standard overflow-reporting subtraction and unsigned magnitude, with exact MiniNumber conversion before dedup insertion. JVM subtracts the smaller value from the larger and rejects negative overflow; MaximaNode uses BigDecimal.longValueExact and passes that validated value into its retained-chat check. There is no global codec change or new dependency.

The ordinary 15-minute Swift and six-hour JVM windows are unchanged, as is the seven-day JVM history exception. Valid past/future messages still dispatch and deduplicate. Rejected Swift timestamps no longer consume replay budget. Replay protection remains bounded and in memory; fresh-message flooding and restart are not solved by this repair. Swift still rejects nonzero MiniNumber scales, matching its preexisting exact-conversion rule; JVM accepts mathematically integral in-range decimals without truncation.

Validation: four new JVM tests pass, with three failing baseline cases; three new Swift tests pass after reproducing the crash and stale-cache eviction separately. Full local family verification passes **331 JVM tests** (core 201, server 58, Cloud 55, Node 4, maxjar 1, wake 6, desktop 6), **63/63 wire vectors**, Android and portal release builds, standalone desktop jar, and Node/Cloud/relay fat jars. All ten compiled classes in the changed MaximaNode/DedupCache families match each of those three fat jars. Swift passes its **76-test release suite** and **36-test simulator app suite**; the reviewed timestamp fixture passes again with a protocol-sized 32-byte nonce. Both Electron RPC/session suites pass five tests each. App and extension build as **0.1.28 build 26**, pass strict deep signature verification, and install/launch successfully on the authorized iPhone.

Code commits: JVM `fe426d38557ee9d90e7bfcb5dd28e240a648b5f9`; Swift `fa31e43c37ce4a7aac73f14a9c5f1705554dae9c`. Both are pushed. [Linux CI 34456620173](https://github.com/eurobuddha/maxima/actions/runs/34456620173) passed for that exact JVM commit. Node 0.2.86 was published through the existing dependency-release channel. Both desktop fetch scripts verified and installed the same jar (SHA256 `ed73c534f1dee6a0d70082cc58e34680d5c3f43d8a6a09b91ed2beb147070301`). minimaCore source 0.16.61 commit `2a66434b9b54f3ed2d51e892edfe7fde53b5dfac` and minimaDesk 0.7.45 commit `886078bd2410f36cacd16d3f591295110590b22b` are committed and pushed. Eight unrelated minimaCore files were hash-preserved; its separate pending manifest remains 0.16.62. No desktop installer release or fleet deployment was performed. Normal iPhone metadata confirms 0.1.28 build 26; no new synthetic test workers remain (the previously documented cache-baseline process is unchanged).

Review verdict: approved for timestamp admission and arithmetic. The decoder and message-layout contracts remain intact, while malformed time values fail locally before application work. Sovereignty gain: peers cannot use numeric edge cases to crash the Swift receiver or evade its host's freshness policy. No bootstrap, relay-selection, custody or authority change; this correctness repair requires no external component. Compatibility risk is limited to malformed timestamp senders, mitigated by the existing Java/Swift wire suites and valid-message regressions. Classic's separate engine retains its existing policy.

## Continuation: Swift mailbox delivery completion — 0.1.29 build 27

### High: the iOS relay could receive deletion permission before RPC handling finished

A signed, encrypted synthetic message sent through the existing loopback AckRelay reproduced an early positive mailbox ACK while its RPC handler was still blocked. The baseline test failed two assertions against `fa31e43c37ce4a7aac73f14a9c5f1705554dae9c` (`/private/tmp/parlons-swift-delivery-baseline.log`). This demonstrates a transport completion defect, not an observed loss of the owner's phone messages.

Reuse review covered JVM `core/src/main/java/com/eurobuddha/maxima/core/MaximaNode.java`, `core/src/main/java/com/eurobuddha/maxima/core/net/HostConnection.java`, their mailbox/dedup completion regressions, and Swift `ParlonsKit/Sources/ParlonsKit/Session/{DeviceNode,HostConnection}.swift`, `Rpc/RpcPeer.swift`, `Push/PushService.swift`, `Account/ParlonsRemote.swift`, the existing ResumeOnce helper and lifecycle tests. The local Swift cancellation/waiter pattern supplies DeliveryCompletion; Java's already-shipped completion ownership and bounded ACK worker design supplies the integration contract. Java CompletableFuture and executor objects cannot be linked into the Swift target, so the adaptation uses existing Swift tasks, locks, timeout helpers and transport fixtures without a new dependency.

DeviceNode registers a shared completion before dispatch. Active duplicates wait for their original even after completed-cache churn or the freshness window expires. Active and cached outcomes stay bounded separately. Known pre-dispatch RPC capacity refusal is forgotten so the same retained message can retry; uncertain failed outcomes remain in the bounded cache. Node shutdown fails active work. RpcPeer owns completion callbacks alongside its tasks and admits at most 256 owned tasks and 256 pending calls per peer. Existing synchronous public entry points remain available.

HostConnection snapshots preceding deliveries when a positive challenge arrives, waits up to ten seconds off the reader, then invokes the installed flush hook before signing the unchanged key/sequence bytes. One task per connection coalesces challenges; a process-local budget admits eight ACK workers. Each connection retains at most 4,096 distinct completion references. Saturation or timeout withholds deletion permission for the relay's normal drain retry; failed delivery closes the attachment. Close cancels owned work and clears retained state. A synchronous flush that ignores cancellation keeps its process slot until it returns, so reconnects cannot evade that ACK-worker limit. Sequence-zero possession proof remains immediate, and ordinary send ACKs continue to progress on the reader.

PushService now awaits asynchronous consumers and shares active event outcomes through its existing 256-entry dedup policy. Account-key authorization still precedes dedup admission. SessionController awaits its guarded MainActor update instead of launching an untracked task. This certifies completion of that update, not the asynchronous history refresh it may trigger.

Validation: **90 Swift release tests** and **36 simulator app tests** pass, including 14 new package tests for shared-waiter cancellation, early/late failure, timeout cleanup, actual RPC completion and reader progress, challenge coalescing, duplicate reconnect/cache churn, node stop, timeout recovery, reference bounds, process-worker saturation/recovery, non-cooperative flush closure, RPC admission/retry, and authorized push completion. The existing app callback test now asserts the MainActor update is already visible when its awaited callback returns. Logs: `/private/tmp/parlons-swift-delivery-final.log`, `/private/tmp/parlons-delivery-app-tests.log` and `/private/tmp/parlons-delivery-iphone-build.log`. Both app and extension build as **0.1.29 build 27**, pass strict deep signature verification, and were installed and launched after announcement. Normal device metadata confirms that exact version; the header reads the bundle version opposite relay status. No new XCTest workers remain; the previously documented old cache-baseline process is unchanged.

Source commit: `39df9bd446508e1bb409fc29ae0eaf7e4d4580a4`, pushed to iOS master. The JVM completion counterpart is already included in Node 0.2.86 and in both verified desktop dependencies recorded above; this continuation changes no JVM source or desktop artifact. Swift's per-peer RPC admission limit is a local adaptation: Java pending-call limits still need review. No desktop installer or fleet release was performed.

Review verdict: approved for the reproduced completion race and bounded local admission. Gain: positive mailbox ACKs cannot bypass tracked unfinished handling, and delivery waits do not block the reader. Sovereignty: all new state is local, with unchanged independently selected relays, owner-controlled bootstrap, identity custody and signed protocol. This is a client correctness repair requiring no external component; relay infrastructure remains replaceable. Risk: overload or failed handling can delay drain/reconnect; explicit bounds, timeout, cancellation and recovery regressions cover those paths.

Limits: dispatcher return and an installed flush hook do not certify RPC reply delivery, separately scheduled application jobs, durable cache writes or exactly-once effects across restart. The app does not install a beforeAck storage hook; its account retains authoritative history. RPC task bounds are per peer and require cooperative handlers to finish; they are not a process-wide bound across retired non-cooperative peers. The ordered-send ACK ledgers in Java/Swift and JVM pending-call admission remain follow-up review targets, alongside cache/cursor coordination.

## Continuation: bounded ordered-send acknowledgement history

### Major: cancelled sends accumulated unbounded ACK slots on an attachment

Java `core/src/main/java/com/eurobuddha/maxima/core/net/HostConnection.java` and Swift `ParlonsKit/Sources/ParlonsKit/Session/AckLedger.swift` preserve a position for each interrupted, cancelled or timed-out attached send. This is necessary because the wire ACK contains no correlation ID: deleting a slot early could attribute its late reply to the wrong send. However, neither implementation limited those retained positions. A relay can withhold ACKs while the caller repeatedly cancels sends; timeout paths also retain positions when unrelated inbound traffic keeps the link alive.

Real loopback regressions using synthetic identities filled 4,096 abandoned slots and demonstrated that both prior implementations still wrote the next message. Java and Swift each failed the baseline assertion with 4,097 actual attached writes (`/private/tmp/parlons-ack-java-baseline.log`, `/private/tmp/parlons-ack-swift-baseline.log`). These are tests of indefinite retained-history growth, not evidence of an observed owner-device memory incident.

Reuse review inspected the complete Swift AckLedger and HostConnection send/close paths, TcpLink/ResumeOnce/MaximaSender, AttachedSendTests/AckRelay, the Java HostConnection and MaximaSender paths, FakeRelay/SendInterruptionTest and the server AttachedSendTest suite. The sibling freezepeach-ios search yielded no compatible ordered Maxima ACK ledger. Both Electron manifests and `scripts/fetch-parlons-node-jar.sh` implementations were inspected for propagation. Existing FIFO ordering, tombstones, failure draining, socket closure, recovery and checksum-fetch implementations are retained; no queue rewrite, protocol extension or dependency is introduced.

Both transports now admit at most **4,096 pending ACK positions per connection**, counting abandoned slots. A full ledger refuses the next frame and retires the attachment through its existing close/death path, releasing waiters and allowing a clean reattachment. Close and ACK admission share a permanent stopped flag under the ledger lock, preventing a closed ledger from accepting a late registration. Swift also refuses identifier exhaustion before UInt64 arithmetic can overflow. Ordinary late ACKs still consume their own positions and free capacity without closing a healthy connection. The existing silent-timeout and cancellation behavior remains unchanged below the limit.

Two Java regressions and three Swift regressions cover exhaustion before another write, late-ACK draining followed by a correctly attributed new reply, and Swift closed-ledger admission. Existing Java interruption and real-server concurrent-send tests and Swift's 256-concurrent-send test still pass. Final verification: **333 JVM tests** (core 203, server 58, Cloud 55, Node 4, maxjar 1, wake 6, desktop 6), **63/63 golden vectors**, Android/portal release builds, standalone desktop jar and all three Node/Cloud/relay fat jars. All three compiled HostConnection class files match byte-for-byte inside each fat jar. Swift passes **93 release tests** and **36 simulator app tests**; app and notification extension build and pass strict deep signature verification as **0.1.30 build 28**. Both Electron RPC/session suites pass five tests each.

Code commits: JVM `a1d8f92a111dde4e21297315d1630c299d224de5`; Swift `50b5e39e35be8618e31c351d78f3d0c44b681c22`, both pushed. [Linux CI 34461158272](https://github.com/eurobuddha/maxima/actions/runs/34461158272) passed for that exact JVM commit. Node 0.2.87 is published through the existing dependency channel. Both desktop fetch scripts verified the release and installed byte-identical jars with SHA256 `b8d10fa9484b991f36d95691d874f4fe772b1c8a112c5b5393c5f7be4a00ad23`. minimaCore 0.16.62 commit `6a63edfbb942ef58bc72ed979b0bb2d2f11e99e8` and minimaDesk 0.7.46 commit `98555b520180215e549a2893fac9eaaa0f6e24dd` are pushed. Eight unrelated minimaCore files remain hash-identical, with its separate pending manifest now 0.16.63. The announced iPhone installation and launch succeeded, and normal device metadata confirms 0.1.30 build 28. No new XCTest workers remain; the documented older cache-baseline process is unchanged.

Verification logs: `/private/tmp/parlons-ack-family.log`, `/private/tmp/parlons-ack-swift-final.log`, `/private/tmp/parlons-ack-app-tests.log`, `/private/tmp/parlons-ack-iphone-build.log`, and the desktop `parlons-ack-core-tests.log`, `parlons-ack-desk-tests.log`, `parlons-ack-core-fetch.log`, `parlons-ack-desk-fetch.log` files in `/private/tmp`.

Review verdict: approved for the reproduced ACK-history growth. Gain: abandoned ACK positions cannot grow indefinitely on a single attachment; Swift's existing linear queue operations are now bounded too. Sovereignty: the limit is entirely local, retains the same signed wire and owner-selected independent relays, and requires no registry, coordinator or external service. Relay infrastructure remains optional and replaceable. Risk: a relay more than 4,096 unacknowledged frames behind causes a reconnect and ambiguous send results; no delivery success is fabricated, and normal caller retry/dedup policy still applies. Late-ACK recovery and existing concurrent-send regressions protect normal operation.

Limits: this is a per-attachment count bound, not a byte quota or a global connection/work budget. Fresh-socket population, JVM pending RPC admission, cross-restart outcomes and total work across retired non-cooperative peers remain separate review areas. It does not make ACKs carry correlation IDs or solve malicious relay behavior generally. No central fallback, funded transaction, cloned live identity, desktop installer release or fleet deployment is involved.

## Continuation: bounded pending RPC calls and isolated expiry callbacks

### Major: Java RPC admission could grow without a pending-call bound

`core/src/main/java/com/eurobuddha/maxima/core/rpc/RpcPeer.java` tracked each outgoing call in an unbounded ConcurrentHashMap until reply, send failure, expiry or close. A synthetic no-network regression filled 255 calls and released eight concurrent callers into the remaining position of the intended 256-call budget. All eight were accepted before the repair. Three new RpcAdmissionTest cases exercise the actual request construction/signing path with an attached-send stub; no live identity or external message is involved.

The admission gate reuses Swift `ParlonsKit/Sources/ParlonsKit/Rpc/RpcPeer.swift` and `ParlonsKit/Tests/ParlonsKitTests/RpcAdmissionTests.swift`, together with Java's existing RpcPeer lifecycle lock. At most **256 pending calls per RpcPeer instance** are now admitted across all target addresses. Excess calls throw through the existing call error contract before MaximaSender builds/signs or sends the request. Replies, expiry, failed sends and close keep their existing removal paths and free capacity. No per-address map, scheduler, protocol field or production dependency is introduced.

Reuse review also covered Java RpcExpiryTest/RpcLifecycleTest/RpcEnvelope/ServiceRegistry, MaximaSender's attached-send behavior, MaximaNode.maintain, account `ParlonsRemote.callOnce`, and `ParlonsControl`'s device-push callers. Account calls already propagate send/admission exceptions through their normal error/retry paths; device pushes use their existing failure accounting and optional wake fallback. Sibling minima-core, freezepeach-ios and desktop searches supplied no more compatible RPC admission implementation.

### Major: one throwing timeout callback could interrupt all following expiry work

RpcPeer.expire removed a due entry and then invoked its callback without containment. A runtime exception stopped iteration and escaped into MaximaNode.maintain before directory expiry and periodic refresh. A second regression, with three expired callbacks that throw, failed against the previous code. Expiry now reuses RpcPeer.close's RuntimeException isolation outside the lifecycle lock, so the other calls still expire. Conditional removal remains unchanged: a reply winning after an expiry snapshot is not followed by a second completion.

Three new tests cover concurrent capacity contention, reply-driven slot reuse, failed-send slot reuse, complete expiry despite throwing callbacks, no repeated expiry and later call admission. Two cases fail on the previous production source (`/private/tmp/parlons-rpc-admission-baseline.log`); all pass after the repair alongside the existing expiry-race and node shutdown tests. The final family gate passes **336 JVM tests** (core 206, server 58, Cloud 55, Node 4, maxjar 1, wake 6, desktop 6), **63/63 wire vectors**, Android/portal release builds, standalone desktop jar and all Node/Cloud/relay fat jars. All three RpcPeer class files match the compiled test outputs inside each fat jar. Both Electron RPC/session suites pass five tests each. Swift's corresponding admission bound is already present in installed iOS 0.1.30 build 28; this pass changes no Swift source and performs no phone installation.

Source commit: `c66635496545c01089fa1ef59b2b8a1c483c01ff`, pushed. [Linux CI 34462434926](https://github.com/eurobuddha/maxima/actions/runs/34462434926) passed for that exact source commit. Node 0.2.88 is published through the existing dependency channel. Both desktop fetch scripts verified and installed byte-identical jars with SHA256 `9b7e8ae023184df062b38e0a9a76700e6f6a23650eb0790fd9a2715195ad178a`. minimaCore 0.16.63 commit `3edf35986814b55c8c43338fac3b02fa746d1b8e` and minimaDesk 0.7.47 commit `da1b52d5083f7b7d3ef6d7ea8f3fc40f6135e84c` are pushed. Eight unrelated minimaCore files remain hash-identical; its separate pending manifest is now 0.16.64. No desktop installer release, fleet deployment or phone operation was performed.

Logs: `/private/tmp/parlons-rpc-admission-baseline.log`, `/private/tmp/parlons-rpc-admission-fixed.log`, `/private/tmp/parlons-rpc-admission-family.log`, `/private/tmp/parlons-rpc-admission-ci.log`, and the corresponding `parlons-rpc-admission-core-tests.log`, `parlons-rpc-admission-desk-tests.log`, `parlons-rpc-admission-core-fetch.log` and `parlons-rpc-admission-desk-fetch.log` files in `/private/tmp`.

Review verdict: approved for the reproduced pending-map growth and interrupted expiry. Gain: missing replies have a local count bound, and a throwing timeout consumer cannot stop the remaining expiry callbacks or subsequent maintenance by propagating a runtime exception. Sovereignty: independently operated relays, owner-selected bootstrap, keys, signed messages and custody remain unchanged. This local correctness/resource limit requires no service or authority; infrastructure remains replaceable. Risk: local overload reports a call failure and may trigger existing retry/device-wake handling. Concurrency and capacity-release regressions verify recovery, while existing race tests preserve single completion.

Limits: the budget bounds pending response records per RpcPeer, not payload bytes, all sockets, all processes or sends whose records have already expired. It does not cancel every in-flight write, establish durable RPC outcomes, or protect maintenance against a callback that blocks indefinitely. Caller-provided deadline arithmetic and retained references after close remain review targets; no claim of complete RPC lifecycle correctness is made.

## Continuation: RPC deadline arithmetic and closed-state ownership

### Major: extreme positive timeout values crashed Swift or expired immediately in Java

Swift `ParlonsKit/Sources/ParlonsKit/Rpc/RpcPeer.swift` added 5,000 milliseconds to an Int timeout without an overflow check. A synthetic public-call test with Int.max terminated XCTest with signal 5 (`/private/tmp/parlons-rpc-deadline-swift-baseline.log`). The Java peer similarly added a long timeout to wall-clock time; Long.MAX_VALUE became an already-expired deadline. The account wrapper `account/src/main/java/com/eurobuddha/maxima/cloud/ParlonsRemote.java` independently overflowed its timeout-plus-grace wait into an immediate TimeoutException. Separate core and account-wrapper regressions failed against the prior sources.

The existing API, five-second grace and ordinary timeout behavior remain intact. Swift uses its existing overflow-reporting arithmetic pattern to saturate an overflowing wait at Int.max. Java uses Math.addExact and saturates the overflowing deadline at the corresponding long limit; the account's positive grace addition saturates at Long.MAX_VALUE. The standard library's timeout/cancellation machinery remains in use. No arbitrary new timeout cap, decoder change or dependency is introduced. This is a local SDK-input defect; the tests do not demonstrate an unauthenticated remote crash path or an incident on the owner's phone.

### Major: a closed Java peer retained newly supplied state and exposed its internal address list

Java RpcPeer setters still accepted an attached sender and reply addresses after close, retaining the sender's captured objects and repopulating state the owner had just cleared. Its getter also returned the actual mutable address list: a caller clearing a returned list changed the next request's advertised reply routes. Regressions reproduce post-close state admission and getter aliasing. Setters now share the existing lifecycle lock and ignore updates after closure; close clears the references under that same lock. The getter returns an independent list snapshot. Existing in-repository callers read the getter and use the setter for changes; no direct getter-mutation caller was found.

Reuse review covered the complete Java RpcPeer and its RpcExpiryTest/RpcLifecycleTest/RpcAdmissionTest fixtures, Java ParlonsRemote.callOnce and its account lifecycle fixtures, Swift RpcPeer/ParlonsRemote/RpcLifecycleTests/RemoteLifecycleTests, and the existing Swift DeviceNode checked-arithmetic pattern. Searches of the shared JVM/account sources and the sibling minima-core sources found no compatible timeout-addition helper to reuse. The lifecycle contract is adapted directly from Swift's guarded setters and value-semantic address arrays; checked addition uses existing language/JDK primitives in the three existing call paths. Both desktop manifest/fetch pipelines remain the propagation mechanism.

Four new JVM tests cover maximum timeout without early expiry, closed setter admission, independent address snapshots, and the actual Java account wait completing after a delayed synthetic reply. All four failed before the repair. One new Swift test reproduces the crash before repair and now verifies successful admission followed by prompt cancellation and pending-call cleanup. Final verification: **340 JVM tests** (core 209, server 58, Cloud 56, Node 4, maxjar 1, wake 6, desktop 6), **63/63 wire vectors**, Android/portal release builds, standalone desktop jar, Node/Cloud/relay fat jars, **94 Swift release tests**, **36 simulator app tests**, and five integration tests in each Electron desktop. The three RpcPeer class files match all three fat jars; the five ParlonsRemote class files also match Node and Cloud. App and extension build as **0.1.31 build 29** and pass strict deep signature verification.

Code commits: JVM `20e6b58bfe1443f6c5b9a26dfe2aac2e3f1e1eee`; Swift `6facfe4336b52aa5a3ea16ad71f435a500035fb9`, both pushed. [Linux CI 34463828500](https://github.com/eurobuddha/maxima/actions/runs/34463828500) passed for that exact JVM commit. Node 0.2.89 is published through the existing dependency channel. Both desktop fetch scripts verified and installed identical jars with SHA256 `fed50241cd20233ffa44554802aaae14eaab4e7c9dc9a804dcb988dc295d2e7a`. minimaCore 0.16.64 commit `07db2f4892447db7591ce4b357a4f3aa10e2c257` and minimaDesk 0.7.48 commit `876c8e995f9aa93316995c40b1497d352d7cb110` are pushed. Eight unrelated minimaCore files remain hash-identical; its separate pending manifest is now 0.16.65. The announced iPhone installation and launch succeeded, and normal app metadata confirms 0.1.31 build 29. No new XCTest workers remain; the previously documented old cache-baseline process is unchanged. No desktop installer release or fleet deployment was performed.

Logs: `/private/tmp/parlons-rpc-deadline-java-baseline.log`, `/private/tmp/parlons-rpc-deadline-remote-baseline.log`, `/private/tmp/parlons-rpc-deadline-swift-baseline.log`, `/private/tmp/parlons-rpc-deadline-java-fixed.log`, `/private/tmp/parlons-rpc-deadline-swift-fixed.log`, `/private/tmp/parlons-rpc-deadline-family.log`, `/private/tmp/parlons-rpc-deadline-swift-final.log`, `/private/tmp/parlons-rpc-deadline-app-tests.log`, `/private/tmp/parlons-rpc-deadline-iphone-build.log`, plus the desktop `parlons-rpc-deadline-core-tests.log`, `parlons-rpc-deadline-desk-tests.log`, `parlons-rpc-deadline-core-fetch.log` and `parlons-rpc-deadline-desk-fetch.log` files in `/private/tmp`.

Review verdict: approved for the reproduced arithmetic and state-ownership defects. Gain: very large positive waits remain positive and cancellable, and Java peer closure cannot be undone by its configuration setters. Sovereignty: arithmetic, references and limits stay local; relay choice, independent operation, owner bootstrap, signed protocol and custody are unchanged. No coordinator or new external requirement is introduced. Risk: callers intentionally mutating a getter result must use the existing setter instead; repository callers were checked, and snapshot/lifecycle tests cover the new isolation. Saturated extreme waits remain subject to explicit close/cancellation and the existing pending-call capacity bound.

Limits: this does not turn wall-clock deadlines into a monotonic-clock policy, cancel all Java in-flight writes, or establish durable RPC outcomes. Very large local timeout requests can intentionally remain pending until reply/close. Broader timeout arithmetic outside these RPC paths and transport ownership across expiry or shutdown remain review areas. No funded transaction, live identity clone or fleet deployment was used.

## Continuation: binary record replacement

### Major: binary writes still used destructive replacement and shared staging paths

`core/src/main/java/com/eurobuddha/maxima/core/store/FileStore.java` used a predictable `<record>.tmp` path for binary records. A later writer could truncate and consume an existing staging file. Its rename failure fallback explicitly deleted the target before retrying, so an unexpected empty directory was removed and reported as a successful write. This was separate from the text writer repaired earlier in the audit.

Binary records now directly reuse the existing private temporary-file writer, file sync, replacement and cleanup sequence. The smallest extraction is a private streaming body callback; text escaping, binary `[keyLen][key][value]` format, one-file-per-item layout, 64 KiB buffering and `putBytes`'s boolean I/O-failure contract remain intact. The mailbox's existing reservation rollback and legacy migration handling therefore continue to receive failure through their established path. No payload-sized serialization copy or new dependency was introduced.

Reuse inspection covered FileStore's complete text writer and failure/coalescing tests, Mailbox's binary write/migration/accounting callers and `MailboxStoreTest`, `AccountFiles.writePrivate`, `BlobStore.put`, Swift `ParlonsKit/Store/CacheStore.swift`, and the embedded Minima runtime's `MiniFile.writeDataToFile`. The same-class text implementation was the compatible tested donor; Swift already delegates private staging to Foundation, while MiniFile's direct overwrite and BlobStore's chunk-specific accounting are different contracts.

Two added cases in `FileStoreFailureTest` both fail against the previous implementation: preserving an unexpected target directory, and leaving a pre-existing staging file untouched. They pass after repair, including retry/reopen, temporary-file cleanup, Unicode keys, arbitrary value bytes, listing and POSIX 0600 checks. The existing six text failure cases, coalescing and mailbox migration/restart/durability tests pass. Final local validation passed **342 JVM tests** (core 211, server 58, Cloud 56, Node 4, maxjar 1, wake 6, desktop 6), **63 wire vectors**, all Android/portal release builds, Node/Cloud/relay fat jars, standalone desktop jar, and **10 Electron integration tests**. Both FileStore class files in all three fat jars match the tested compiled output. Final source review found no blocking issue in this change.

Source commit `132dbe0aa9c8d151a8c20336a4dda752f24e78ba` is pushed. [Linux CI 34465409238](https://github.com/eurobuddha/maxima/actions/runs/34465409238) passed for that exact commit. Node 0.2.90 is published through the existing dependency channel. Both desktop fetch scripts verified and installed byte-identical jars with SHA256 `3f8565e6e12a2dc78a3c88e29a36e0146e12cbb9fa0102fe1ec9f134ef146066`. minimaCore 0.16.65 commit `785f78734cf9207649926ea3a67b6d81a9736001` and minimaDesk 0.7.49 commit `ba425fb6b22bdc1838c0e9593601646c959f3a99` are pushed. Eight unrelated minimaCore source files remain hash-identical, with its separate pending manifest now 0.16.66. The phone remains on the previously verified 0.1.31 build 29; the Swift cache uses Foundation's atomic writer and needed no change in this JVM repair. No desktop installer release was performed.

**Sovereignty:** the gain is consistent replacement safety for locally held encrypted mail, preserving independent relay operation, operator-owned storage, wire compatibility and local custody. This is local shared-library behavior, with no new required service or authority. Risk: the non-atomic-filesystem fallback retains its existing weaker crash guarantees; file sync and private staging reduce exposure, but directory sync, arbitrary power loss and cross-process transaction isolation are not established by these tests. Read/header validation, ignored deletion failures, cached partial reads, append durability and BlobStore persistence remain separate review areas. No claim is made that all persistence failures are repaired. No funded transaction, live identity clone, fleet deployment or phone operation was used for this change.

## Continuation: unread mail and cumulative acknowledgements

### High: a failed relay read could delete mail that was never delivered

`server/src/main/java/com/eurobuddha/maxima/server/RelayServer.java` skipped a null `Mailbox.Item.ciphertext()` result and continued sending later records. `FileStore.getBytes` returns null on an I/O error as well as a missing file. The resulting challenge used the highest later sequence sent, while the signed acknowledgement deletes every held item through that sequence. A transient read failure could therefore become permanent message loss even with a correctly behaving client.

The drain now stops at a null read, reusing its adjacent stop-on-exception behavior. It may acknowledge the prefix already sent, but cannot advance that drain's challenge past the failed item. Restoring the record allows the next drain to send the remaining items. The stored format, signed acknowledgement bytes, proof-of-possession checks, existing expiry/quota policy and Java/Swift client behavior are unchanged.

Reuse inspection traced FileStore's read contract, Mailbox's on-demand payload reads and cumulative removal, RelayServer's full drain/ACK path, Java and Swift HostConnection's challenge handling, and the existing `MailboxDurabilityTest`, `MailboxCompletionTest` fixture, `RelayHardeningTest` signed-ACK setup and `AttachedSendTest` transport helpers. `MailboxReadTest` adapts the existing captured-frame and signed-reply techniques at the relay end, using real FileStore records and synthetic identities/transport. No production API was added for testing.

Both regression cases fail against the old source: an unreadable first item still produces challenge 3 instead of no challenge, and an unreadable middle item produces challenge 3 instead of 1. Answering those actual challenges with the owner's valid signature clears all three records in the old-code run, including the unread record. The repaired cases verify the safe prefix, retained count, byte-identical redelivery after restoring storage, final signed deletion and an empty reopened binary store. Existing application durability and delivery-completion tests also pass.

Final local validation passed **344 JVM tests** (core 211, server 60, Cloud 56, Node 4, maxjar 1, wake 6, desktop 6), **63 wire vectors**, Android and portal release builds, Node/Cloud/relay fat jars, standalone desktop jar and **10 Electron integration tests**. All five RelayServer class files in each fat jar match the tested compiled output. Final review found no blocking issue within this read-gap repair.

Source commit `4c989a5affc369e2d01155049d4238558a3f405f` is pushed. [Linux CI 34497908387](https://github.com/eurobuddha/maxima/actions/runs/34497908387) passed for that exact commit. Node 0.2.91 is published through the existing dependency channel. Both desktop fetch scripts verified and installed identical jars with SHA256 `96cefe60b25568a1f0b788c9ed9f30d1eb8c65fe8e820b57492d9ca4b660316a`. minimaCore 0.16.66 commit `86cf2f32bf4cda852da17362b3b2546d7627f70e` and minimaDesk 0.7.50 commit `55f50cec1cfab44ccf1e46b58dd15246579748d2` are pushed; both contain only their two manifests. iOS remains at the previously installed 0.1.31 build 29 because this repair changes only the relay. No desktop installer release was performed.

The minimaCore preservation snapshot matched all eight unrelated files immediately before the desktop commits. The post-push check detected seven paths had subsequently changed to match committed HEAD: `main/casino.js`, `main/casino/engine.js`, `main/casino/service.js`, `main/main.js`, `main/preload.js`, `renderer/app.js` and `scripts/casino-parity-check.sh`. This repair did not write or restore those paths; the actor behind the intervening changes was not established. The current five integration tests were rerun and pass. The changelog remains unchanged from the pre-pass snapshot, and the separate pending working-tree manifest is 0.16.67. Pre/post hashes are retained in `/private/tmp/parlons-mailbox-read-core-preserved.json` and `/private/tmp/parlons-mailbox-read-core-after.json`; no claim is made that all unrelated bytes remained constant throughout concurrent work.

**Sovereignty:** the gain is retaining operator-held encrypted mail through a failed read, preserving independent relays, owner storage, custody, signatures and existing interoperable clients. This is local relay behavior, with no new central dependency or protocol authority. Risk: a persistently unreadable record can delay later records for that recipient until recovery or existing expiry; stopping at the failure preserves data and the existing periodic drain can retry. This does not establish correctness of concurrent drain/write snapshots, sequence reuse, ignored deletion failures, binary header validation, corrupt startup indexes, cross-process state or arbitrary disk rollback. Those require separate evidence and remain in the audit scope. No live identity, funded transaction, phone operation or fleet deployment was used.

## Continuation: pending mailbox writes and the Tier 1 RPC path

### High: later completed writes could expose earlier pending mail to deletion

Mailbox persistence deliberately happens outside the mailbox monitor. However, `Mailbox.fetch` previously returned a later committed item while an earlier sequence was still being written. If that earlier write completed between fetch and the cumulative acknowledgement, the acknowledgement could remove the earlier item without delivering it.

`core/src/main/java/com/eurobuddha/maxima/core/mailbox/Mailbox.java` now retains the existing reserved Item in its bounded pending-ID map. Fetch finds the earliest pending sequence and publishes only committed items before it. Success and failure both remove the reservation through the existing completion path; other recipients, duplicate admission checks, caps and disk writes outside the monitor remain intact. The map reuses the already allocated Item, and the scan is bounded by the existing per-recipient item cap; it does not copy ciphertext or introduce another work queue.

The existing `MailboxStoreTest.GatedStore` is the regression donor. Its test-only gate/result are now captured before signalling entry, eliminating a fixture race where a later call's gate could release an earlier writer. Three new cases exercise an earlier pending write, an independently acknowledgeable prefix, and a failed earlier write that releases the later item. All three fail on the old source. The repaired tests also cover duplicate admission, other-recipient progress, the write completing between fetch and ACK, retained payload order and worker cleanup. The original outside-monitor, durability, eviction and migration tests continue to pass.

### High: the RPC mailbox repeated the relay's skip-on-read-failure defect

`core/src/main/java/com/eurobuddha/maxima/core/services/Tier1Services.java` provides a separate `mailbox.fetch`/`mailbox.ack` interface. Its fetch loop also skipped null reads and returned later rows, allowing a correctly formed cumulative RPC acknowledgement to clear unread records. It now reuses RelayServer's stop-at-read-failure behavior. Requester-derived mailbox ownership, RPC names, row format, cursor and policy checks are unchanged.

`MailboxRpcReadTest` adapts the existing `LiveTier1Test` fetch/ack flow and the previous relay read-failure fixture at the registered handler boundary. Real temporary FileStore records are made unreadable; the simulated client acknowledges the highest row actually returned. Both first-record and middle-record cases delete the unread record on the old implementation and fail their retained-count assertions. The repair preserves the safe prefix and verifies recovery, exact row bytes, final acknowledgement and an empty reopened store. These are local handler tests using already-verified synthetic sender metadata, not a new test of network signature validation.

Reuse inspection covered Mailbox's reserve/write/commit, fetch/ACK, load ordering and eviction paths; the complete GatedStore fixture and callers; Tier1Services' registration/ownership handlers; ServiceRegistry dispatch; RelayServer's drain and signed-ACK path; and the Java/Swift challenge consumers. Search found no separate Swift Tier 1 mailbox host implementation requiring this JVM repair. Both shared retrieval entry points are covered by the mailbox publication rule.

The first full-family run stopped on two existing mesh fixtures (`MeshForwardTest.nonPoolRelayDoesNotForward`, `MeshReplicateTest.anAcceptedSetIsReplicatedAndResolvesWithTheAnchorDown`) with `BindException: Address already in use` at relay startup. Both fixtures obtain ports by binding and closing temporary sockets before relay startup, leaving a reservation race. Both passed their isolated recheck. Original failure XML and logs are retained under `/private/tmp/parlons-mailbox-order-*`; the fixture port-allocation weakness remains an open test-infrastructure issue rather than being hidden by the later result.

The final full-family run passed **349 JVM tests** (core 216, server 60, Cloud 56, Node 4, maxjar 1, wake 6, desktop 6), **63 wire vectors**, Android and portal release builds, Node/Cloud/relay fat jars, standalone desktop jar and **10 Electron integration tests**. All five Mailbox/Tier1Services class files in the three fat jars match the tested compiled output. Review of the final production changes, fixtures and callers found no blocking issue within this publication/read-gap repair. The initial fixture startup failures remain recorded above.

Source commit `70d4553c3f8073dfd10e4f40676339d0a13c5fa4` is pushed. [Linux CI 34499877855](https://github.com/eurobuddha/maxima/actions/runs/34499877855) passed for that exact commit. Node 0.2.92 is published through the existing dependency channel. Both desktop fetch scripts verified and installed identical jars with SHA256 `4b506ffb07163d12abcb811bd591aba53801b093f2ffc92cad6fe53d2685437e`. minimaCore 0.16.67 commit `bb31fcc232b58a8cab824c4afe4041131c8d3ccc` and minimaDesk 0.7.51 commit `6725dcfa9377e1de6cc78d4fd2c1c50d7edd190f` are pushed; both contain only their two manifests. All eight unrelated minimaCore paths match this pass's starting snapshot after the commits/pushes, with its separate pending manifest now 0.16.68. The preceding pass's concurrent source changes remain documented above. iOS stays at the previously installed 0.1.31 build 29. No phone operation, desktop installer release or fleet deployment was performed.

**Sovereignty:** the gain is consistent cumulative-acknowledgement safety across relay and device-contributed mailbox retrieval, preserving independent storage contributors, custody, signatures, relay choice and existing wire formats. This is bounded local state, with no required coordinator or new external service. Risk: a slow pending write delays later delivery for its own recipient; the existing prefix and other recipients remain available, and write completion/failure releases the barrier. Arbitrarily throwing Store implementations, sequence reuse after box recreation, stale ACKs, overlapping drain snapshots, failed deletion accounting, startup corruption and cross-process durability remain separate review areas. No live identity, funded transaction, phone operation or fleet deployment was used.

## Continuation: failed text reads must not become empty state

### High: a failed collection read could overwrite a valid saved snapshot later

`core/src/main/java/com/eurobuddha/maxima/core/store/FileStore.java` caught collection-read I/O errors, logged them and returned its partial/empty map from `computeIfAbsent`. That map was then cached as writable state. If storage recovered, a later put could replace the valid file with the failed snapshot plus the new record. Log reads similarly returned partial/empty data after an I/O error.

Both text readers now use FileStore's existing `UncheckedIOException` failure contract. A failed collection load exits `computeIfAbsent` without populating the cache, so a later operation retries the disk read. Presence checks now use `Files.notExists` with `NOFOLLOW_LINKS`: an uninspectable path or a dangling file link is not automatically treated as a missing file. Actual missing files and valid empty snapshots remain supported. Existing text formats, escaping, write-behind retry, atomic replacement and binary-record behavior are unchanged.

The reuse search covered FileStore's load/write and read/rewrite contracts and its failure/coalescing tests, the shared `walletfile/com/eurobuddha/wallet/FileKeyUses.java` reader and tests, MaximaNode settings/contact/history callers, ChatEngine loading, PeerDiscovery's load/save, account/Android/desktop startup wiring, and Swift `ParlonsKit/Store/CacheStore.swift`. FileStore's own exception and filesystem-fixture patterns were the compatible building blocks. The Swift store is a best-effort account-data cache with a different authority contract; no Swift implementation was substituted for authoritative JVM state.

Three new `FileStoreFailureTest` cases fail on the old code: a blocked collection read followed by storage recovery loses a previously saved record after the next put; an unavailable parent path is reported as an empty collection/log; and an unreadable log returns normally. The repair verifies retained data after retry/reopen, rejection of reads/put/remove while the parent is unavailable, no rejected mutation left for a later flush, restored escaped log data, and normal missing-file behavior. Original old-code XML is retained at `/private/tmp/parlons-store-read-baseline.xml`. These tests inject real open failures; they do not inject a physical disk error halfway through a stream.

The initial full run exposed an existing fixture dependency: the application-flush case in `MailboxDurabilityTest` injected its write obstruction before its first collection load, so the new reader correctly failed earlier than that fixture intended. Flush cases now preload their valid initial snapshot, preserving their existing flush-failure assertions. A separate new read-failure case uses the same real loopback relay/node fixture: unsuccessful application delivery closes the attachment and leaves mail held; storage restoration and reconnection retry the failed delivery, save it and grant deletion only after successful processing/flush. Attempt counts distinguish a failed application retry from an already-completed delivery repeated merely for flush recovery. The focused durability/completion suite passes with both paths covered. The initial failing report is retained under `/private/tmp/parlons-store-read-*`.

The final full-family run passed **353 JVM tests** (core 219, server 61, Cloud 56, Node 4, maxjar 1, wake 6, desktop 6), **63 wire vectors**, Android and portal release builds, Node/Cloud/relay fat jars, standalone desktop jar and **10 Electron integration tests**. Both FileStore class files in all three fat jars match the tested compiled output. Final review of the production diff, affected callers and recovery fixtures found no blocking issue within this I/O-failure repair. The initial durability-fixture failure is retained above.

Source commit `36660b4ff1abfae3c77b37083587a71a4ab0b155` is pushed. [Linux CI 34502159578](https://github.com/eurobuddha/maxima/actions/runs/34502159578) passed for that exact commit. Node 0.2.93 is published through the existing dependency channel. Both desktop fetch scripts verified and installed identical jars with SHA256 `02524d736cede86ede187895cd4be9de29e6aaadf41dcc394186cce115c0b212`. minimaCore 0.16.68 commit `dc9aa6a3812edbc25596778803d712642f86281f` and minimaDesk 0.7.52 commit `97a4279a1bdabde4354cbe497e69cdadd731302e` are pushed; both contain only their two manifests. All eight unrelated minimaCore paths match this pass's starting snapshot after commit/push, with its separate pending manifest now 0.16.69. iOS remains at the previously installed 0.1.31 build 29. No phone operation, desktop installer release or fleet deployment was performed.

**Sovereignty:** the gain is preserving authoritative local records through a read failure, with owner-controlled storage, independent hosts, keys and protocol unchanged. This is local error handling with no added service, registry or authority. Risk: unreadable saved state now stops affected initialization or operations until storage is repaired; this deliberately avoids replacing it from an empty view. The FileStore retry/recovery tests and real-loopback mailbox test cover the repaired paths. Malformed-record parsing, empty-key round trips, invalid text encoding, binary-header validation, deletion/append failures, cross-process changes and arbitrary disk rollback remain separate review areas. No live identity, funded transaction, phone operation or fleet deployment was used.

## Continuation: preserve empty records and reject damaged text

### High: silent format repair could discard or alter authoritative records

`core/src/main/java/com/eurobuddha/maxima/core/store/FileStore.java` wrote empty keys but skipped them when reopening a collection, and wrote empty log entries but discarded them on reading. Rows without a separator were silently dropped; unknown escapes lost their backslash; unfinished escapes were accepted. The default UTF-8 reader replaced malformed input bytes. A later mutation could therefore persist a silently altered view of the original file.

Reuse-first inspection covered FileStore's full reader/writer and failure contract, its failure/coalescing tests and callers, the JVM `core/src/main/java/com/eurobuddha/maxima/core/util/Json.java` parser, `walletfile/com/eurobuddha/wallet/FileKeyUses.java` and its validation tests, and the sibling Swift `../support/parlons-ios/ParlonsKit/Sources/ParlonsKit/Store/CacheStore.swift`. JSON and properties grammars are different and do not supply a compatible store parser. Searches found no existing strict UTF-8 reader utility. The minimal adaptation retains FileStore's escaping, buffering, atomic writer and failed-read cache behavior: accept a separator at offset zero, retain empty log lines, use the standard UTF-8 decoder's reporting behavior, and reject missing separators or invalid/unfinished escapes through the existing `UncheckedIOException` contract. Blank collection lines remain tolerated. No file migration or wire-format change is needed for valid writer output.

Five new `FileStoreFormatTest` regressions cover empty and escaped Unicode keys/values, reopen/removal, empty entries through log rewrite/append, damaged rows/escapes, invalid UTF-8, preservation of original damaged bytes, and repair/retry through the same uncached instance. All five failed against the previous production class; the baseline XML is `/private/tmp/parlons-store-format-baseline.xml`. Focused storage and real-loopback mailbox durability tests passed after the repair. The full final gate passed **358 JVM tests**, **63 wire vectors**, both Android release builds, Node/Cloud/relay fat jars, standalone desktop jar and **10 Electron integration tests**. Both FileStore class files match the tested compiled output in all three fat jars. Code review: approved for these format-integrity cases; failed parsing cannot publish its partial map and valid writer escaping still round-trips.

Source commit `4229f0a52b59361958bbd14ea6f188eabe149ce3` is pushed. [Linux CI 34504023890](https://github.com/eurobuddha/maxima/actions/runs/34504023890) passed for that exact commit. Node 0.2.94 is published through the existing dependency channel. Both desktop fetch scripts verified identical jars with SHA256 `2058e5e91db8941484a5eaf0c7f83e09ae0933b90e9adbd6fef666cf2d726e53`. minimaCore 0.16.69 commit `b134696dfdd514dc8b461223a8c7c2e4022ddeaf` and minimaDesk 0.7.53 commit `02ac40c8c9f3599fb7360b3ab2c8f6b942100815` are pushed; each contains only its two manifests. All eight unrelated minimaCore paths match this pass's starting snapshot after commit/push, with its separate pending working-tree manifest now 0.16.70. No desktop installer release or fleet deployment was performed.

**Sovereignty:** the gain is preserving the owner's local records instead of silently discarding or changing them. Storage, custody, independently selected relays and the signed protocol remain unchanged. This local parser requires no service, authority or registry. Risk: malformed saved text now stops affected reads/initialization until the owner repairs the file; the original bytes are retained and repair/retry is tested. The implementation remains locally replaceable. Unpaired Java surrogate inputs during writes, binary-header validation, deletion/append failures, cross-process edits, disk rollback and automatic repair remain separate review areas. iOS uses its separate JSON cache and remains at the previously installed 0.1.31 build 29. No live identity, funded transfer, phone operation, desktop installer or fleet deployment was used.

## Continuation: mailbox cursors must survive recipient removal

### High: delayed acknowledgements could delete newer mail after sequence reuse

`core/src/main/java/com/eurobuddha/maxima/core/mailbox/Mailbox.java` allocated sequences inside each `Box`, starting at one. Acknowledgement removed an empty Box, global quotas evicted whole boxes, and startup expiry removed old records. Recreating a recipient therefore reused earlier cursors, and restarting an empty durable mailbox lost all cursor history. A delayed cumulative acknowledgement could delete a new record that had never been delivered; a client resuming after its old cursor could also miss the new record.

Reuse-first inspection covered the complete Mailbox implementation, `MailboxStoreTest`, `MailboxRpcReadTest`, the relay's `MailboxReadTest`, `RelayServer` drain/signature verification, `Tier1Services` fetch/ack handlers, `Store` and FileStore's atomic/failed-read behavior. The closest counter donor was `walletfile/com/eurobuddha/wallet/FileKeyUses.java` and its full test suite: its wallet-specific integer counters, mirrored properties files and wallet dependency do not directly fit pure-core mailbox sequences. Java `core/src/main/java/com/eurobuddha/maxima/core/net/HostConnection.java` and Swift `../support/parlons-ios/ParlonsKit/Sources/ParlonsKit/Session/HostConnection.swift` already sign and compare positive 64-bit cursors without requiring consecutive values. The desktop integrations consume the shared Node jar.

The fix uses one local sequence allocator across a Mailbox, backed by the existing Store API and FileStore atomic writes. One `mailseq.tsv` keyed record reserves up to 4096 numbers synchronously before any number in that batch can publish an item. The unused remainder is skipped on restart. A failed reserve/flush or exhausted counter returns `IO_ERROR` before adding an item or pending reservation; malformed saved counters stop loading and leave admission disabled. Startup advances and persists the floor from existing binary record keys before expiring or pruning them. Empty/evicted recipients leave no permanent per-recipient counter map. Pending-write publication barriers and ciphertext I/O outside the mailbox monitor remain intact; the small counter write occurs under that monitor once per reservation batch.

Seven initial regressions all failed against the previous production class (`/private/tmp/parlons-mailbox-sequence-baseline.xml`): empty-recipient reuse, eviction, empty-store restart with write-behind, startup expiry of the last old record, failed reservation flush/recovery, invalid saved counters, and exhaustion without wrap. An eighth test runs 5000 recipient admissions/acknowledgements across a batch boundary, confirms one metadata record and at most four keyed reservation writes, then verifies restart/cursor progress. Two integration regressions exercise a repeated old ACK through the existing RPC handler and through actual relay challenge signing and frame capture. The existing restart test now requires a strictly newer cursor rather than exactly previous+1; its initial assertion failure is retained in `/private/tmp/parlons-mailbox-sequence-fixed.log`. All other existing mailbox concurrency, read-failure, flush and delivery fixtures passed.

Final local validation passed **368 JVM tests**, **63 wire vectors**, both Android release builds, Node/Cloud/relay fat jars, standalone desktop jar and **10 Electron integration tests**. All four compiled Mailbox class files match in all three fat jars. Review: approved for cursor reuse within a running mailbox and across restarts of its intact, single-owner durable store. Sequence admission, reservation failure, upper-bound arithmetic and compatibility with both client languages were checked.

Source commit `6f8eab653aeadf8431bc163931168a68fd4034ce` is pushed. [Linux CI 34505804722](https://github.com/eurobuddha/maxima/actions/runs/34505804722) passed for that exact commit. Node 0.2.95 is published through the existing dependency channel. Both desktop fetch scripts verified identical jars with SHA256 `e2df79c2a2e85829084e4b977b9f2efebc5a7c1058ee491e084c7aa4478c06f4`. minimaCore 0.16.70 commit `ea439b5f4b95c98d9046238a837d2396f1441760` and minimaDesk 0.7.54 commit `7add558b7950f1aa76f8b901d32d015c35541274` are pushed and each contains only its two manifests. All eight unrelated minimaCore paths match the starting snapshot after commit/push, with its separate pending working-tree version now 0.16.71. No desktop installers or fleet updates were deployed.

**Sovereignty:** the gain is preventing old delivery permissions from addressing newly admitted mail, with constant local metadata and amortized writes. All custody, independent relay selection and operation remain local/replaceable; there is no shared network counter, authority, registry or mandatory service. Risks: a batch reservation briefly holds the mailbox monitor during its small durable write; unavailable metadata stops admission instead of permitting cursor reuse. The counter must accompany the mailbox directory in backups. Cursors now reflect a host-wide allocation order, which can expose aggregate activity between a recipient's observations; skipped reservations/restarts mean the difference is not an exact delivered-message count. Content and recipient keys remain protected by the existing protocol.

Limits: this cannot reconstruct cursor history already deleted by pre-upgrade versions. Deleting, rolling back or concurrently writing the same storage directory is outside the single-owner Store contract; an intentionally memory-only mailbox keeps the guarantee only for its lifetime. The unchanged signed ACK canonical bytes do not add relay/session nonce binding, so broader cross-session/cross-relay replay analysis remains open. Arbitrary Store exceptions during ciphertext writes, binary deletion failures, and overlapping drain ordering remain separate audit targets. No live identity, funded transfer, phone operation, desktop installer or fleet deployment was used; iOS stays at the previously installed 0.1.31 build 29.

## Continuation: thrown storage writes must release mailbox reservations

### High: a write exception permanently stranded later mail and leaked capacity

`core/src/main/java/com/eurobuddha/maxima/core/mailbox/Mailbox.java` reserved global/per-recipient item and byte budgets plus a pending content ID, then called `Store.putBytes` outside its monitor. A `false` return reached rollback; a runtime exception bypassed it. The failed content ID remained permanently pending, retry was seen as a duplicate, later committed records stayed behind the publication barrier, and global budgets remained charged even if the recipient was subsequently evicted.

Reuse-first inspection covered Mailbox's complete admission/commit/rollback path, `core/src/junit/java/com/eurobuddha/maxima/core/mailbox/MailboxStoreTest.java` and its gated FileStore adapter, the Store binary defaults and FileStore failure tests, `Tier1Services` and `RelayServer` result handling, and sibling Java/Swift/Electron cleanup candidates. The existing mailbox rollback is the compatible implementation. `putBytes` runtime failures now log and flow through that same `IO_ERROR` path, undoing pending IDs, pending counts/bytes and global reservations. An empty failed-admission box is removed using identity-conditional map removal, so cleanup cannot delete a newer box for the same recipient. This also removes empty boxes after the existing false-return failure path. Ciphertext I/O remains outside the mailbox monitor.

Three new regressions all failed against the prior production class (`/private/tmp/parlons-mailbox-throw-baseline.xml`). They verify same-message retry under one-item/one-byte quotas, visibility of later completed mail after an earlier gated write throws, and global accounting when a throwing writer's box has already been evicted. The fixture captures each invocation's exception before announcing entry, preserving the existing per-write gate ownership. Tests observe the leaked state rather than merely expecting an exception, join every new worker and require the final `IO_ERROR` result. The existing false-return fixture now also asserts that no empty box is retained.

Focused mailbox sequencing, read-failure and real-loopback durability fixtures passed. Final local validation passed **371 JVM tests**, **63 wire vectors**, both Android release builds, Node/Cloud/relay fat jars, standalone desktop jar and **10 Electron integration tests**. All four Mailbox class files match the tested output inside all three fat jars. Code review: approved for runtime write-exception reservation cleanup, including concurrent publication and eviction; no duplicate rollback path or new protocol/state format was added.

Source commit `8e6c78cea770f3f54aacabd85a79602863f277e4` is pushed. [Linux CI 34507163284](https://github.com/eurobuddha/maxima/actions/runs/34507163284) passed for that exact commit. Node 0.2.96 is published through the existing dependency channel. Both desktop fetch scripts verified identical jars with SHA256 `9ff9f9230278800bd7a1fb680c4c174f18fcca99000211c6ffa440ed9aa5ef42`. minimaCore 0.16.71 commit `445c8569355689c1eeb254401308b88fc765d826` and minimaDesk 0.7.55 commit `162517567ba1cde0bf7b39d051f361ba569afda3` are pushed; each contains only its two manifests. All eight unrelated minimaCore paths match the starting snapshot after commit/push, with its separate pending working-tree version now 0.16.72. No installers or fleet updates were deployed.

**Sovereignty:** the gain is recovering local mailbox capacity and delivery progress after an adapter failure, preserving owner-controlled storage, keys, independent relays and existing retry behavior. This is local, replaceable error handling with no required service, coordinator or authority. Risk: an exception is now reported as the established storage-error result rather than escaping the mailbox call; it remains logged, and relay callers can answer UNKNOWN so senders retry. Fatal JVM Errors and indefinitely blocked stores are outside this recovery path. An adapter that commits bytes and then reports failure can still leave an uncertain durable outcome; binary deletion failures, restart reconciliation of such outcomes and cross-process storage ownership remain open. No live identity, funded transfer, phone operation, desktop installer or fleet deployment was used; iOS stays at the previously installed 0.1.31 build 29.

## Continuation: binary records must match their keys and representable lengths

### High: damaged binary metadata could return the wrong record or overflow allocation sizes

`core/src/main/java/com/eurobuddha/maxima/core/store/FileStore.java` read and discarded the stored key without comparing it with the requested key. Its key-buffer allocation came from the file header, and it narrowed the pathname's value length from long to int without checking representability. Listing decoded invalid UTF-8 with replacement characters, accepted keys under unrelated filenames, overflowed large lengths, and omitted valid empty keys written by the same class.

Reuse-first inspection covered FileStore's complete binary writer/reader/listing, `Store` defaults, mailbox callers and failure/recovery tests, `BlobStore.java`, `MediaCodec.java`, searches of sibling Swift cache and Minima utility trees. Blob/media records use a different content-addressed format. The compatible block is the existing keyed binary format and hashing. Reads now compare header length to the expected UTF-8 key length before allocating the stored-key buffer, compare key bytes before returning a value, and validate value lengths before narrowing. Both reads and listings take their length from the opened file channel rather than the potentially replaced pathname. Listings retain valid empty keys, decode UTF-8 strictly and verify the filename using the existing key hash. Invalid records are omitted/return null through the existing binary failure contract; reading does not rewrite them.

The filename helper now reuses `core/src/main/java/com/eurobuddha/maxima/core/codec/Hex.java`, adapting only its prefix and letter case. SHA-256 always supplies a nonempty 32-byte digest, so the helper's empty-input quirk is irrelevant. This preserves every existing lowercase filename while removing 32 `String.format` calls per hash. The full Hex source, its codec/parity checks and existing mailbox fixtures that independently calculate filenames were inspected. `Reads.exact` was also inspected; its growing-buffer approach addresses untrusted stream-declared lengths, while this repair validates an opened file's size and the caller's expected key without adding an extra ciphertext copy.

Six new `FileStoreBinaryReadTest` cases cover key mismatch and repair, sparse oversized values without large physical allocations, an inflated key header, empty keys/values and Unicode reopen/listing, invalid UTF-8, and truncated headers/keys. Five failed on the previous production class, including a `NegativeArraySizeException`; the truncation control already passed. Baseline evidence is `/private/tmp/parlons-binary-read-baseline.xml`. All storage/mailbox focused tests passed after the fix. Final validation passed **377 JVM tests**, **63 wire vectors**, both Android release builds, Node/Cloud/relay fat jars, standalone desktop jar and **10 Electron integration tests**. Both FileStore class files match the tested output in all three fat jars. Code review: approved for these identity, decoding and checked-length paths, with the limits below.

The initial [Linux CI run 34508600673](https://github.com/eurobuddha/maxima/actions/runs/34508600673) for binary-fix commit `73f9d52a605e7e5ed2e76a9272e1880bd0ec42e7` failed in `MailboxDurabilityTest` while awaiting recovery deletion. Its uploaded XML records the cause: the fixture swept at now+90001 ms while the reattached socket was writing, and the relay logged a 90-second stalled writer and reaped it. Source inspection confirmed that a new connection's `lastDrain` is zero, so the first periodic drain is already due at real current time. Reusing the current-time sweep exercised in `RelayHardeningTest`, `MailboxDurabilityTest`, `MailboxCompletionTest` and `MailboxAgeTest` now use the actual time for these first drains. Production stall detection is unchanged and its deliberately aged-writer test still passes. This test correction is a separate patch/version commit, `dc8c2527181400c53bfcf626b446a743ac0c2984`; the final local family gate passed again (`/private/tmp/parlons-binary-read-family-final.log`). Failed CI evidence is retained under `/private/tmp/parlons-binary-read-ci-reports`.

Final [Linux CI 34509279362](https://github.com/eurobuddha/maxima/actions/runs/34509279362) passed for exact commit `dc8c2527181400c53bfcf626b446a743ac0c2984`. Node 0.2.98 is published through the existing dependency channel; the intermediate 0.2.97 jar was not published. Both desktop fetch scripts verified identical jars with SHA256 `7f32ccbb96c55a21d498ea51a7f8c461e9a8a9bb3d2ddb0e18611e9317c6b521`. minimaCore 0.16.72 commit `bfff4134eb806e92e87c6d3877c595c02d4738a3` and minimaDesk 0.7.56 commit `f5fd6c03668b4359589b6781a1999cbd765a4118` are pushed and each contains only its two manifests. All eight unrelated minimaCore paths match the starting snapshot after commit/push, with its separate pending working-tree version now 0.16.73. No installers or fleet updates were deployed.

**Sovereignty:** the gain is consistent local record identity and safe length conversion, with no custody, relay-selection, protocol or infrastructure change. Existing owner-controlled files and replaceable hosts remain authoritative; no new service, coordinator or registry is required. Listing now hashes each bounded key to validate its filename, adding metadata work without reading ciphertext; shared hex encoding removes per-byte formatting overhead. Risk: damaged metadata that was previously accepted becomes unavailable until repaired; tests verify the original file remains intact and valid replacements work.

Limits: the existing 4096-byte listing-key bound is unchanged, while the writer still permits longer keys; that API mismatch remains open. The value API still returns a byte array, so a large but representable value can exceed available heap; this pass prevents narrowing overflow and allocation from a mismatched key header, not every possible resource exhaustion. Binary body authentication remains the caller's responsibility. Concurrent in-place mutation, uncertain writes/deletions, invalid Java surrogate inputs and cross-process store ownership remain separate review areas. No live identity, funded transfer, phone operation, desktop installer or fleet deployment was used; iOS stays at the previously installed 0.1.31 build 29.

## Mailbox read budgets and ciphertext integrity continuation

### High: damaged ciphertext could advance cumulative acknowledgements

`core/src/main/java/com/eurobuddha/maxima/core/mailbox/Mailbox.java` knew each held item's size and SHA3 content ID, but returned disk bytes without either check. A file enlarged after admission could also cause `FileStore.getBytes` to allocate beyond that item's accepted size. Three new real-file RPC regressions reproduce enlargement, truncation and same-length alteration on the previous code: all three advanced a cumulative ACK and removed the affected record. The unchanged control cases passed. Baseline evidence is `/private/tmp/parlons-mailbox-bounded-baseline.xml` and its sibling log.

Reuse-first inspection covered `Store.java`, `FileStore.java`, the complete relevant mailbox read/admission methods and tests, `Tier1Services.java` and `RelayServer.java` drain callers, `Reads.java`, `MediaCodec.java`, `BlobStore.idOf`, `Hashes.java`, `MediaTest.java`, the Swift `ParlonsKit/Sources/ParlonsKit/Account/Media.swift`, and both desktop Node fetch scripts. `Reads.exact` avoids allocating from short stream declarations but cannot impose the mailbox's accepted size on a file that really has grown. The existing FileStore reader now accepts a caller byte budget and checks it before value allocation. The original two-argument method delegates with its existing int limit; a default Store overload preserves older adapters with a documented post-read check. Mailbox supplies `Item.size`, requires the exact length, and reuses its admission hash and the media content-ID validation pattern. No binary format or wire change is needed.

The existing relay/RPC null-read barriers retain damaged and later records. Recovery tests restore the original bytes and prove the remaining rows are returned unchanged and can then be acknowledged. Two direct FileStore API cases cover exact fits, empty/missing records, oversized representable sparse files, negative budgets and replacement recovery; another case checks compatibility with the existing GatedStore adapter. Six tests were added in total. The full local gate passed **383 JVM tests** (core 248, server 62, Cloud 56, Node 4, classic maxjar 1, wake 6, desktop 6), **63 wire vectors**, both Android release builds, and Node/Cloud/relay/desktop jars. The existing media executable passed **12 checks**, and both Electron integrations passed their five tests each. The first standalone media invocation omitted the resources directory and stopped at the BIP39 resource lookup; correcting its classpath passed without any source change. Eight changed class-family outputs match all three fat jars byte-for-byte. Evidence: `/private/tmp/parlons-mailbox-bounded-{fixed,family,media-final,minimacore-desktop-tests,minimaDesk-tests}.log`.

Code-review verdict: approve for the reproduced disk corruption and read-budget paths. **Sovereignty:** the gain is bounded local reads and preservation of held mail when its bytes are damaged. Custody, independent relay selection and signed ownership acknowledgements remain unchanged; every check runs on the owner's chosen host, without a coordinator, registry or external authority. This is a required local integrity check in the updated code, with replaceable storage implementations and unchanged peer compatibility. The cost is one existing SHA3 hash per disk delivery read. Damaged rows intentionally block later cumulative delivery until repaired or handled by the existing expiry/eviction policy; automatically skipping them would reintroduce the loss demonstrated by the regression.

Limits: this is content consistency against the stored ID, not authentication against an attacker able to replace both data and metadata. The accepted size after restart still comes from the bounded mailbox listing, and owner-controlled quotas remain the global bound. The generic Store compatibility default checks only after its old reader returns; only FileStore provides the pre-allocation bound here. The old unbounded binary API remains available to other callers. Memory-only byte-array ownership, concurrent in-place mutation, rollback, uncertain writes/deletions and multi-process ownership are separate review areas. No phone operation, live identity, funded transfer, fleet deployment or desktop installer was used in this pass.

Code commit `1a8da0bb139a7414982eab902381ed58732e367b` is pushed and its exact [CI run 34511088383](https://github.com/eurobuddha/maxima/actions/runs/34511088383) passed. [Node 0.2.99](https://github.com/eurobuddha/maxima/releases/tag/node-v0.2.99) is published with SHA256 `c424b3fd9557023c90cfb92806c1ff312ae17df10c5c3a128ba7a19ecd757818`; both existing desktop fetch scripts verified that identical artifact. minimaCore 0.16.73 commit `71b1d42b61bff79e41721b4cd49356ad85d767e3` and minimaDesk 0.7.57 commit `d6e62a5fb45d9d11f7712eb219a34b25d0629ebc` are pushed and contain only their two manifests. All eight unrelated minimaCore paths match the starting snapshot, and its separate pending working-tree version is now 0.16.74. Shared JVM source/build versions are Android 0.6.114 code 714, portal 0.2.61 code 105, Swing 1.5.93, Cloud 0.11.96 and relay 0.4.94. iOS remains on the previously installed 0.1.31 build 29; the Swift client has no local relay mailbox disk reader requiring this patch.

## Peer-discovery persistence continuation

### High: concurrent saves could lose or overwrite discovered gateway changes

`core/src/main/java/com/eurobuddha/maxima/core/session/PeerDiscovery.java` saved mutable maps without serializing against intake or another save, then unconditionally cleared `mDirty`. A gateway update during flush could therefore be forgotten at shutdown, and an older blocked put could overwrite a newer completed save. A failed explicit save that began clean did not mark itself for retry. `addSelf` removed a verified endpoint from memory without scheduling its persisted removal.

Reuse-first inspection covered the complete discovery implementation and its persistence/lifecycle tests, `MaximaNode` callers, `FileStore` mutation/flush behavior, `ChatEngine.flushState` with `ChatPersistenceFailureTest` and `ChatRemovalPersistenceTest`, classic and fork `P2PManager.updateP2PPeersList`, and the sibling Swift HostPool/SeedRelays paths. The repair reuses the chat persistence pattern: serialize saves with state mutation, snapshot peer/gateway records, consume only the current dirty marker before storage, and restore it on failure. A reentrant storage callback's new dirty marker survives. Self-address removal now marks the peer list dirty and clears its associated gateway. Coarse timestamps, changed-record-only writes, random peer selection, independent probe verification, and the classic nonempty/half-loaded-size save guard are unchanged.

Four new regressions failed on the prior code and pass after repair: gateway change during flush, older save after newer save, retry of an initially clean explicit save, and self-address removal. A fifth final case proves a reentrant store callback's gateway update survives shutdown. Fixtures reuse `PeerDiscoveryTest.discovery()` with synthetic offline peer records, a real temporary FileStore and the existing proxy/latch and filesystem-obstruction patterns; no public relay or live identity is used. Baseline XML/log: `/private/tmp/parlons-discovery-baseline.xml` and `/private/tmp/parlons-discovery-baseline.log`. Existing discovery lifecycle, random-selection and outage tests pass. Full-family validation passed **388 JVM tests** (core 253, server 62, Cloud 56, Node 4, classic maxjar 1, wake 6, desktop 6), **63 golden vectors**, both Android release builds and all host jars. Both desktop integrations passed five tests each. All four PeerDiscovery class outputs match Node, Cloud and relay jars. Evidence: `/private/tmp/parlons-discovery-{fixed,family,minimacore-desktop-tests,minimaDesk-tests}.log`.

Code-review verdict: approve for the reproduced save-consistency and retry faults, with the storage-latency trade-off explicit. **Sovereignty:** the gain is reliable local persistence of independently discovered peers and gateways, preserving the owner's alternatives across restart. No coordinator, registry, authority, bootstrap dependency, protocol change or new worker is introduced. The correction is part of the local implementation; storage and relay infrastructure remain owner-controlled and replaceable. Saves now hold discovery's existing monitor for their storage duration. Slow or hung storage can delay discovery intake or shutdown; this pass establishes consistency, not a hard latency bound. Coarse timestamps and writes only for changed records limit routine I/O. Save scheduling and the outage guard remain unchanged, so a very small/empty list still deliberately preserves the older usable snapshot.

Limits: disk rollback/corruption, atomic replacement of an entire multi-record snapshot, saved-list admission bounds and broader peer-poisoning/Sybil resistance require separate review. This does not modify the Swift client's different seed-preference persistence. iOS remains on its previously installed 0.1.31 build 29 with the version label. No installer, phone operation or fleet deployment is part of this pass.

Code commit `4eb01018f994d6e7378903296f10f86dd940aee6` is pushed and its exact [CI run 34512642065](https://github.com/eurobuddha/maxima/actions/runs/34512642065) passed. [Node 0.2.100](https://github.com/eurobuddha/maxima/releases/tag/node-v0.2.100) is published with SHA256 `3be14f96b3aebd99bd004b55a5624fd5ad77831cc03c87046e564e07cd9ccfac`; both desktop fetch scripts verified that identical jar. minimaCore 0.16.74 commit `5c20e7b1975d36d48d13c76c6e981b46801f88c3` and minimaDesk 0.7.58 commit `c4d7ef3841c1a9d2252b31be4ed53e313cb50f3b` are pushed and contain only their two manifests. All eight unrelated minimaCore paths match the starting snapshot, with its separate pending working-tree version now 0.16.75. Shared source/build versions are Android 0.6.115 code 715, portal 0.2.62 code 106, Swing 1.5.94, Cloud 0.11.97 and relay 0.4.95. No new desktop installers were released.

## Optional wake-proxy response handling continuation

### High: an unused response body could consume memory and hold the wake worker

`account/src/main/java/com/eurobuddha/maxima/cloud/WakeProxyClient.java` used `HttpResponse.BodyHandlers.ofString()` but inspected only the status code. It accumulated response text without an application body limit and waited for that unused body before servicing the next queued wake or applying failure backoff. Three local HTTP regressions reproduce the wait: a 64 MiB declared response held after its first byte, an unfinished chunked response, and unfinished HTTP 500 responses. All three timed out on the prior reader; the existing three coalescing/off/backoff controls passed. These are synthetic response fixtures, not a claim of measured production heap exhaustion.

Reuse-first inspection covered the complete WakeProxyClient and its tests, the authenticated `ParlonsControl` APNs registration and wake dispatch, `wakeproxy/WakeHandler.java` and `ApnsClient.java`, iOS `APNsRegistrar.swift`, relevant module dependencies, and searches across the sibling Minima/APK/support/desktop trees. No compatible status-only reader was found. The APNs sender needs its body to detect BadDeviceToken; the proxy's bounded reader validates incoming requests, a different contract. The account retains its existing HttpClient, request construction, redirect refusal, quiet/coalescing/backoff rules and test fake. The minimal new integration uses the JDK streaming body handler and closes the body immediately after headers, before applying status logic. The [JDK body-handler contract](https://docs.oracle.com/en/java/javase/21/docs/api/java.net.http/java/net/http/HttpResponse.BodySubscribers.html#ofInputStream()) documents availability after headers, release through close, and the possible loss of HTTP connection reuse. No library or custom stream subscriber was added. The class comment now reflects the existing 45-second quiet period rather than the stale five-minute description.

All six wake-client tests pass. The new cases prove a different chosen proxy progresses while the first body remains unfinished, and three HTTP errors trigger the existing backoff without waiting for their bodies. Fake proxies use four owned test workers so intentionally held responses do not serialize the server itself; close stops them. Baseline: `/private/tmp/parlons-wake-response-baseline.xml` and its sibling log. The final full-family gate passed **391 JVM tests** (core 253, server 62, Cloud 59, Node 4, classic maxjar 1, wake proxy 6, desktop 6), **63 golden vectors**, both Android release builds, and all host jars. Both desktop integrations passed five tests each. The tested WakeProxyClient class matches both Node and Cloud fat jars. Evidence: `/private/tmp/parlons-wake-response-{fixed,family,minimacore-desktop-tests,minimaDesk-tests}.log`.

Code-review verdict: approve for the unused-body allocation/wait path. **Sovereignty:** the gain is less memory and worker occupancy from an optional proxy's response. The chosen endpoint, off switch and content-free token/env/kind payload remain unchanged; message content stays on the owner's account. No coordinator, registry, central fallback, new authority or required service is introduced. The change is local to the account implementation; proxy infrastructure remains optional and replaceable. Early closure can discard connection reuse, trading a possible later handshake for avoiding an unnecessary body download. Success still means the proxy returned a 2xx status, not that APNs delivered a notification.

Limits: the JDK still owns protocol buffers, header parsing and connection/request timing; this is not a zero-allocation or total-resource-bound claim. Tests exercise local HTTP/1.1, not live APNs delivery or every HTTP/2 transport condition. The separate APNs sender's parsed response, wake queue/map admission bounds, and broader shutdown races remain review targets. iOS and the deployed optional proxy need no source change for this account-side reader repair; iOS remains on its previously installed 0.1.31 build 29 and the proxy on source version 0.1.3. No live identity, funded transfer, phone operation or fleet deployment was used.

Code commit `1efdc5491ba3d0c23d43ede06c0c828f4d70f108` is pushed and its exact [CI run 34514067968](https://github.com/eurobuddha/maxima/actions/runs/34514067968) passed. [Node 0.2.101](https://github.com/eurobuddha/maxima/releases/tag/node-v0.2.101) is published with SHA256 `5053e02d07452b8c1d88bdc305d5f80f751b60809f7d088396dccff19d80ef44`; both desktop fetch scripts verified that identical jar. minimaCore 0.16.75 commit `90bbb1be37ae9939c695ce138c4a9993f978b028` and minimaDesk 0.7.59 commit `1a5c56c5e2adc45629d5ca50b8bf969113d680b3` are pushed and contain only their two manifests. All eight unrelated minimaCore paths match the starting snapshot, with its separate pending working-tree version now 0.16.76. Coordinated family source/build versions are Android 0.6.116 code 716, portal 0.2.63 code 107, Swing 1.5.95, Cloud 0.11.98 and relay 0.4.96. The behavioral fix is in the shared Node/Cloud account module, propagated into both desktop Node dependencies. No new desktop installers were released.

## Independent-relay outage continuation

### High: an unreachable anchor bypassed permanent-address fallback

`core/src/main/java/com/eurobuddha/maxima/core/MaximaNode.java:805` tried the directory embedded in a permanent MAX# address before asking its attached pool. A failed reply reached the existing two-answer quorum, but a socket connection exception escaped immediately. `MlsClient.resolve` delegates to `MaximaSender.send`, whose connect failures throw. This made the original relay an unintended availability dependency even when other relays held the publisher's signed record. The account remote login and Android raw-address send paths call this resolver.

The graph query used `seed relay bootstrap mesh resolve pool failover`; its truncated result was a map, with source authoritative. Reuse inspection covered MaximaNode's resolver/pool/quorum and callers, MlsClient request nonce/key validation and signed publish, MaximaSender transport, HostPool attachment and addresses, SeedRelays composition and tests, MeshReplicateTest, RelaySharedModeTest and AttachedSendTest. The Swift sibling `support/parlons-ios/ParlonsKit/Sources/ParlonsKit/Account/ParlonsRemote.swift` already catches ordinary anchor errors and retains cancellation. Java now uses that same minimal pattern, retaining the existing quorum, timeouts, nonce/key validation and attached transport. If fallback cannot resolve, the original transport exception is retained as the failure cause. Interruption still propagates directly.

`IndependentRelaysTest` combines the existing signed replication scenario, shared-mode listener handoff and encrypted delivery collector. Its loopback listeners keep their dynamically allocated ports bound, avoiding the older fixtures' freePort()/close/rebind race. With built-in seeds disabled, a synthetic client starts from its own relay, the original anchor stops, one fallback answer is refused, a second configured relay attaches, the unchanged MAX# resolves and encrypted content arrives. Both replicas carry the publisher's proof. A separate interruption case checks no fresh socket is opened. The first test failed on old code with ConnectException at the final resolution; the interruption control passed. Baseline XML is `/private/tmp/parlons-independent-baseline.xml`. The final negative assertion also checks that the anchor exception remains available as the cause when there is no quorum.

All **393 JVM tests** passed (core 253, server 64, Cloud 59, Node 4, classic maxjar 1, wake proxy 6, desktop 6), along with **63 golden vectors**, both Android release builds and all host jars. Both Electron integrations passed five tests each. All seven MaximaNode class files match the tested core output in Node, Cloud and standalone relay jars. Evidence: `/private/tmp/parlons-independent-{baseline,fixed,family,minimacore-desktop-tests,minimaDesk-tests}.log`.

Code-review verdict: approve for the reproduced anchor transport failure. **Sovereignty:** the gain is recovery through the user's existing relay choices when the original address anchor is unavailable. Built-in seeds remain optional and replaceable; the fixture never needs them. No registry, coordinator, operator fallback or new authority is introduced. The existing two-answer requirement is unchanged. Two endpoints agreeing does not prove two distinct operators control them; Sybil resistance and malicious peer selection remain separate review areas. The replica tests retain signed ownership validation, while client MLS responses retain the existing nonce/key checks; this repair does not introduce end-to-end client verification of replica proofs. Additional fallback queries add bounded per-query network work after an anchor exception; this is no hard total-latency guarantee.

The Swift resolver already has the relevant error handling, so iOS requires no source change and remains at its previously installed 0.1.31 build 29, including the visible version label. No phone operation, live identity, funded transfer or fleet deployment was used. The underlying Minima runtime also needs no source change.

Code commit `02785898cc5df64f717f93db44b4f2bdfc7b3989` is pushed and its exact [CI run 34516211522](https://github.com/eurobuddha/maxima/actions/runs/34516211522) passed. [Node 0.2.102](https://github.com/eurobuddha/maxima/releases/tag/node-v0.2.102) is published with SHA-256 `c089b36a44cbc863effedaa3c01c6faec275ec92077655c63aaefe7b309d20e3`. Both desktop fetch scripts verified that checksum, and both resource jars are byte-identical to the release. minimaCore Desktop source 0.16.76 is pushed as `063f233b73005efae4b5301088d838035b15eaf7`; minimaDesk 0.7.60 is pushed as `8dc0de77946240d914f5d2b382de2fa89503ca45`. Each commit changes only its two manifests. Eight preserved minimaCore paths were byte-checked before and after propagation; its separate pending working-tree version is 0.16.77. No desktop installer release was triggered.

## Account wake admission and retained-state continuation

### High: the account wake queue and histories were unbounded, and queued work bypassed new backoff

`account/src/main/java/com/eurobuddha/maxima/cloud/WakeProxyClient.java` used `Executors.newSingleThreadExecutor` with its unbounded queue. Quiet/coalescing and proxy backoff were checked only at admission. Already queued requests continued posting after the worker learned that a proxy had failed three times. Device timestamps and proxy failure/backoff entries also accumulated until close. This path is driven by authorized paired-device registrations and account message/call events; these findings do not establish an unauthenticated remote registration bypass.

Three regressions failed on old code: a held response allowed work beyond 256 queued items, eleven admitted wakes all reached a failing proxy rather than stopping after three failures, and rotating failed endpoint URLs exceeded a 256-record retention limit. All six existing wake tests passed. The baseline XML and log are `/private/tmp/parlons-wake-admission-baseline.xml` and `/private/tmp/parlons-wake-admission-baseline.log`.

Reuse inspection covered the complete WakeProxyClient and tests, authenticated registration and both wake dispatch paths in ParlonsControl, DevicePairing registration/authorization, WakeHandler and its bounded-queue test, RateLimit's per-key buckets, HostPool's bounded shed executor and admission rollback, and the Swift APNsRegistrar and desktop fetch/concurrency implementations. The graph query (`wake APNs queue response device token backoff`) was truncated and served only as a source map. The account reuses WakeHandler's bounded ThreadPoolExecutor pattern with one worker, HostPool's rejected-admission cleanup pattern, and RateLimit's bucket shape. The proxy bucket adds pending-work ownership so expiry cannot remove an endpoint while queued/running work still references it. No suitable shared implementation combined this client's quiet periods, backoff and pending ownership; the adaptations stay local to the existing client rather than adding a library or service.

Admission now allows one running and at most **256 queued** wakes, at most **4,096 device records** and **256 proxy records**. Refusal returns false without setting a new quiet/coalesce timestamp or retaining a newly refused proxy. Expired device state is removed only after both windows permit it. Proxy records expire after five idle minutes, with no pending work and no active backoff; this also resets an incomplete old failure streak. Active state is never evicted to make room for new keys. Queued work rechecks backoff before HTTP dispatch. State changes and close use one monitor, and late completion cannot repopulate cleared maps. The existing status-only HTTP response closure, content-free payload, redirect refusal, five-second request settings, three-failure/five-minute backoff and user-selected endpoint remain in place.

All **11 wake-client tests** pass. Additional cases exercise exact expiry boundaries, retained backoff at capacity, a full device table without thousands of irrelevant HTTP requests, pending work surviving an expiry sweep, and shutdown releasing state without late repopulation. A test clock makes expiry deterministic. The test-only drain helper retains its executor barrier and retries admission when the queue is full; it does not mistake an unstarted worker for a drained queue. The full family gate passed **398 JVM tests** (core 253, server 64, Cloud 64, Node 4, classic maxjar 1, wake proxy 6, desktop 6), **63 wire vectors**, both Android release builds and all host jars. Both desktop integrations passed five tests each. WakeProxyClient and its ProxyState class match the tested account output in both Node and Cloud fat jars. Evidence: `/private/tmp/parlons-wake-admission-{fixed,family,minimacore-desktop-tests,minimaDesk-tests}.log`.

Code-review verdict: approve for account-side admission, retention and queued-backoff handling. **Sovereignty:** resource use stays local and bounded by counts, with no central registry, service or admission authority. The optional proxy remains replaceable/disableable; message contents remain on the owner's account. Capacity refusal can omit an advisory wake during overload; it neither deletes the stored message nor reserves a quiet period, so subsequent events can retry and normal catch-up remains available. Pending work pins endpoint history; idle cleanup does not erase active backoff. The failure-streak reset after five idle minutes is intentional. This is not a total heap-byte or latency bound: record strings and HTTP implementation buffers still have their own costs, and the existing single worker can delay other endpoints behind a slow request. Expiry uses the existing wall-clock model, so a clock rollback may extend retention but cannot exceed the new count limits.

The separate APNs client's parsed response remains unbounded and needs its own reader review; this change does not claim to fix it. Proxy-side rate-limit key collisions/retention policy, request-reader timeouts, and live APNs delivery also remain review areas. iOS already registers the same protocol and needs no source change; it remains at installed 0.1.31 build 29, including the version label. The optional deployed proxy has no source change in this pass and remains at source 0.1.3. No phone operation, live identity, funded transfer or fleet deployment was used.

Code commit `ef7d318323190c52b418d8e7e8e532b0bc3a2124` is pushed and its exact [CI run 34518068956](https://github.com/eurobuddha/maxima/actions/runs/34518068956) passed. [Node 0.2.103](https://github.com/eurobuddha/maxima/releases/tag/node-v0.2.103) is published with SHA-256 `96924e2eb8ba879774d68e941445e5946f3b70e7ecdbf8419d6fbc9bba221fd5`. Both desktop fetch scripts verified that checksum and both resource jars match the release. minimaCore Desktop source 0.16.77 is pushed as `d741d5a1d8608240b7d30d719b53118bdae07e0c`; minimaDesk 0.7.61 is pushed as `aed8b923005673e5d5bc0d506e9984e4fdcb5307`. Each commit changes only its two manifests. Eight preserved minimaCore paths were byte-checked before and after propagation; its separate pending working-tree version is 0.16.78. No desktop installer release was triggered.

## APNs response bounds and completion deadline continuation

### High: an unfinished APNs body could hold a send worker beyond the configured timeout

`wakeproxy/src/main/java/com/eurobuddha/maxima/wake/ApnsClient.java` used `HttpResponse.BodyHandlers.ofString()` with no application body limit. Its ten-second HttpRequest timeout did not bound body completion in the local regression: an unfinished small error still held the call after twelve seconds. Declared oversized and chunked oversized errors likewise waited for the held body. These three new tests failed on old code; existing production/sandbox retry behavior and an exactly 4 KiB complete error body passed. The baseline is `/private/tmp/parlons-apns-response-baseline.xml` and its sibling log. This proves local response-processing failures, not a claim that Apple's production service supplied these synthetic responses or that heap exhaustion was measured.

Reuse inspection covered ApnsClient, ApnsJwt, Main, WakeHandler and WakeProxyTest, the account's status-only WakeProxyClient, core UpnpIgd's bounded reader and HostConnection's timed future waits, desktop streaming readers, module dependencies, and searches for BodySubscriber/BodyHandler implementations across maxima and the sibling core/APK/support/desktop projects. The graph query was truncated and used as a source map. No compatible bounded HTTP/2 reader existed: the account reader intentionally ignores bodies, whereas APNs error reasons control the one-time environment retry; UpnpIgd uses HttpURLConnection and returns truncated text. The existing HttpClient, JWT, content-free payload and retry policy are retained. A small local BodySubscriber adapts the bounded streaming approach to the required Java HTTP/2 API; it introduces no library, application executor or timer service.

Error bodies are capped at **4,096 bytes**. A declared larger body fails before collection; incremental checks also reject chunked or understated bodies before copying beyond the cap. Rejected prefixes never become parsed responses. Successful HTTP 200 replies do not wait for an unused body. Each gateway attempt waits at most the existing **ten-second response budget** through body completion; a valid BadDeviceToken response may still cause one separate attempt at the other gateway. Timeout/interruption cancels the subscriber and any unfinished response future, and an abort before subscription causes a later subscription to be cancelled immediately. The response subscriber serializes delivery and abort transitions; the byte budget is checked using remaining capacity to avoid count overflow.

The [Java BodySubscriber contract](https://docs.oracle.com/en/java/javase/11/docs/api/java.net.http/java/net/http/HttpResponse.BodySubscriber.html) requires consuming or cancelling unwanted data and documents that early cancellation may prevent connection reuse. The [Java HttpClient API](https://docs.oracle.com/en/java/javase/11/docs/api/java.net.http/java/net/http/HttpClient.html) supplies the existing shared HTTP/2 client and asynchronous response future. The implementation explicitly cancels the body subscription rather than relying only on future cancellation.

All **15 wake-proxy tests** pass, including nine ApnsClient cases: production/sandbox retry in both directions (never more than once), exact-limit ordinary errors, declared/chunked overflow, incomplete-body timeout, immediate success despite an unfinished body, interrupted response completion, cumulative chunk accounting and cancellation before a late subscription. The full family gate passed **407 JVM tests** (core 253, server 64, Cloud 64, Node 4, classic maxjar 1, wake proxy 15, desktop 6), **63 wire vectors**, both Android release builds and all host jars. Both desktop integrations passed five tests each. The standalone jar's classes match the tested wake module and `java -jar ... -v` reports 0.1.4. Evidence: `/private/tmp/parlons-apns-response-{fixed,family,minimacore-desktop-tests,minimaDesk-tests}.log`.

Code-review verdict: approve for the reproduced body bounds and wait failures. **Sovereignty:** the gain is bounded body collection and released optional-proxy send workers. No coordinator, registry, central fallback or new required service is introduced. Users can retain their chosen optional proxy or disable wakes; the content-free protocol and account-side message storage remain unchanged. Early cancellation may reduce connection reuse. The limits cover application body bytes and each response wait, not all JDK transport/header buffers, scheduler delays, cryptographic work or a hard total wake wall-clock bound. The explicit environment retry can require two response budgets. Tests use loopback HTTP/1.1 gateways plus direct subscriber lifecycle cases; they do not establish live APNs delivery or complete HTTP/2/TLS transport coverage. The existing reason parser remains unchanged apart from reading bounded UTF-8 bytes.

**Propagation boundary:** this is the standalone optional proxy, not an embedded Node/Cloud/relay component. Module dependencies and all three jar inventories confirm no `com/eurobuddha/maxima/wake/` classes are bundled there. Both Electron repositories still pin Node 0.2.103 and both resource jars retain its published SHA-256 `96924e2eb8ba879774d68e941445e5946f3b70e7ecdbf8419d6fbc9bba221fd5`. Their integration suites passed without a protocol or manifest change; no desktop dependency bump is needed for this isolated service repair. Android, portal, Swing, Cloud, Node and iOS source versions are unchanged. The proxy source becomes 0.1.4. No live identity, phone operation, funded transfer, installer release or fleet deployment was used.

Code commit `6761984415e41163224e05657219fbc8049e2b29` is pushed and its exact [CI run 34519768658](https://github.com/eurobuddha/maxima/actions/runs/34519768658) passed. [Wake Proxy 0.1.4](https://github.com/eurobuddha/maxima/releases/tag/wake-v0.1.4) is published with SHA-256 `769ed00ed4fcf0347da71bb6f72d7a9014976222af475b80588726f995d4be65`. The published jar was downloaded into `/private/tmp/parlons-apns-response-published` and verified byte-for-byte against the tested staged artifact and both checksum files. This publishes the repair for operators; the running optional service was not redeployed. The standalone proxy's rate-limit key collisions/retention policy and request-reader timeouts remain review targets.

## Wake token isolation and rate-state pressure continuation

### High: truncated token keys shared allowances, and capacity clearing reset active limits

`wakeproxy/src/main/java/com/eurobuddha/maxima/wake/WakeHandler.java` used only the first four bytes of a SHA-256 token digest as both the rate-bucket key and log identifier. A bounded local search over synthetic valid hex tokens found two different full digests with the same old key. A real loopback handler regression reproduced the effect: the first token received 202, while the unrelated second token received 429. `RateLimit.java` also cleared its whole map after adding a 200,001st key, removing active per-token limits along with stale history.

Four new checks failed on old code: the handler collision, capacity pressure resetting an exhausted 60-per-hour allowance, expiry pressure erasing a still-active ten-second gap across the hour boundary, and full unexpired state admitting a new key by clearing retained limits. A global-cap control passed. Capacity fixtures populate the real public admission path with synthetic timestamps and fewer than 3,000 requests per minute; no reflection or live service is needed. One test explicitly models wall-clock rollback, then verifies recovery after forward expiry. Baselines: `/private/tmp/parlons-wake-limit-baseline-rate.xml`, `/private/tmp/parlons-wake-limit-baseline-handler.xml` and their sibling log.

Reuse inspection covered WakeHandler, RateLimit and WakeProxyTest; account WakeProxyClient's expiry-only cleanup and strict capacity refusal with its tests; RelayServer's keyed rate maps and maintenance; NodeGateway's RateLimiter/Bucket; and sibling Swift/desktop rate/hash searches. The graph query was truncated and used only as a source map. The repair retains the proxy's existing synchronized bucket algorithm, thresholds, global window, token canonicalization and SHA-256 encoder. The encoder now emits every digest byte, complying with the family's full-identifier rule. Retention adopts the account client's policy: remove only entries whose hour window AND minimum gap have both expired; refuse a new key if the map remains at its 200,000-record cap. Existing keys keep their active allowances. A full-map expiry scan runs at most once per global minute window. The global attempt counter stops increasing at 3,000 rather than continuing indefinitely after refusal.

The HTTP regression verifies independent allowances for the two synthetic tokens, refusal of an uppercase repeat of the same token, complete expected digests in logs, and absence of raw token strings. The old claim that the digest identified nothing was corrected: it is a stable pseudonym, not anonymity. No token hash is truncated for display or admission. All **20 proxy tests** pass. The full family gate passed **412 JVM tests** (core 253, server 64, Cloud 64, Node 4, classic maxjar 1, wake proxy 20, desktop 6), **63 wire vectors**, both Android release builds and all host jars. Each desktop integration passed five tests. The staged standalone jar matches the compiled proxy classes and reports 0.1.5. Evidence: `/private/tmp/parlons-wake-limit-{fixed,family,minimacore-desktop-tests,minimaDesk-tests}.log`.

Code-review verdict: approve for the reproduced token-isolation and rate-state reset faults. **Sovereignty:** limits remain local to an optional replaceable proxy; no identity registry, central admission authority, coordinator or required service is introduced. The full digest prevents the demonstrated four-byte-prefix collision from merging distinct token allowances. SHA-256 is not a mathematical proof that collisions are impossible. Full hexadecimal keys use more memory per record than the old short strings, but retained record count is capped. Preserving active windows can refuse new keys at capacity; after one full scan, another scan waits for a new global minute window, potentially delaying admission even if an entry expires sooner. This bounds cleanup work rather than evicting active limits. The existing wall-clock/global-limit model remains, including extended refusal after a clock rollback; this is not a fairness or Sybil-resistance guarantee.

**Propagation boundary:** only the standalone optional proxy changes to 0.1.5. Node, Cloud and relay jar inventories again confirm that they do not bundle `com/eurobuddha/maxima/wake/` classes. Both desktop repositories still pin Node 0.2.103 and their resource jars match its published SHA-256 `96924e2eb8ba879774d68e941445e5946f3b70e7ecdbf8419d6fbc9bba221fd5`. Their protocol and integration checks pass without manifest changes. All other source versions, including iOS 0.1.31 build 29, are unchanged. No live token, owner identity, funded transfer, phone operation, installer release or fleet deployment was used.

Follow-up source candidates identified during reuse inspection: `node/src/main/java/com/eurobuddha/maxima/node/NodeGateway.java` clears all per-IP buckets at its tracking limit; `server/src/main/java/com/eurobuddha/maxima/server/RelayServer.java` removes expired keyed rate entries at its threshold but still inserts when all entries are active. Their refill/window semantics differ from this proxy. They need targeted reproduction and repairs in their own contexts; this pass does not claim they are fixed. Proxy request-reader timeouts and live APNs HTTP/2/TLS behavior also remain review areas.

Code commit `72c47fa924726a1a8ed3ba1ff8c2eb5e7c172fb9` is pushed and its exact [CI run 34521297221](https://github.com/eurobuddha/maxima/actions/runs/34521297221) passed. [Wake Proxy 0.1.5](https://github.com/eurobuddha/maxima/releases/tag/wake-v0.1.5) is published with SHA-256 `8a480073e3873abd7cc631f5515826b8a31fd4292b6ac9ec2f15e02128a5bd4c`. The release jar was downloaded into `/private/tmp/parlons-wake-limit-published` and verified byte-for-byte against the tested staged artifact and both checksum files. This publishes the repair for operators; the running optional service was not redeployed.

## Gateway and relay rate-state capacity continuation

### High: gateway capacity reset active allowances; relay capacity was not enforced

`node/src/main/java/com/eurobuddha/maxima/node/NodeGateway.java` cleared all 8,192 per-IP buckets when an unknown IP arrived at capacity. This reset exhausted clients along with idle history. `server/src/main/java/com/eurobuddha/maxima/server/RelayServer.java` described a 50,000-entry cap but only removed expired entries before inserting another key; a fully active table could continue growing. Its probe helper duplicated that policy. Refused requests also kept incrementing the relay's integer counter, eventually wrapping it into an accepted value. Replica-source rate state was absent from periodic cleanup.

Six regression failures were reproduced on the original production code: gateway allowance restoration and full-table admission; general relay and probe over-cap admission; relay counter overflow; and expired replica-source state remaining after maintenance. Two gateway controls passed. Baseline evidence is `/private/tmp/parlons-host-limits-baseline-{node,relay}.xml` and `/private/tmp/parlons-host-limits-baseline.log`. The fixtures use synthetic key/IP labels, an unstarted synthetic relay and reflected timestamps/counter state; they do not send traffic to a live operator or touch an owner identity. Reflection makes the integer-overflow and long-expiry cases bounded instead of issuing billions of requests or waiting days.

Reuse inspection covered the complete gateway bucket/limiter and both endpoint callers, relay rate tables/callers and the existing 30-second RelayRuntime maintenance loop, Node test setup, RelayHardeningTest, the optional proxy's tested capacity/expiry handling, and relevant Swift/desktop/full-node sibling searches. The graph query was used as a source map, not proof of behavior. The gateway retains its monotonic token refill and global/per-IP budgets; a limiter monitor now serializes lookup, capacity admission, acquisition and retirement. At capacity it retires only fully refilled buckets, at most once per second, and refuses new IPs if no space is available. Removing a fully refilled bucket grants no extra tokens when that IP returns. Partially refilled buckets remain intact. The `/cmd` and `/nft` limiters remain separate.

The relay retains its existing keyed minute-window helper and operator-set rates. Admission, accounting and cleanup share the table monitor. Unknown keys are refused at the exact cap; known keys retain their remaining allowance. Refused requests stop incrementing the counter. Probe admission now delegates to the same helper. The existing maintenance loop reclaims expired minute windows from all nine rate tables, including replica input and the single-key forward/replication budgets. Full tables do not trigger a scan on each rejected request.

All **12 new regression cases** pass, including concurrent attempts at the last available slot, partial gateway refill retention, fully refilled bucket reclamation, relay recovery after minute expiry, future timestamps after wall-clock rollback, and existing global/disabled gateway controls. The full family gate passed **424 JVM tests** (core 253, server 70, Cloud 64, Node 10, classic maxjar 1, wake proxy 20, desktop 6), **63 golden wire vectors**, both Android release builds and all host jars. Each Electron integration passed five RPC/session checks. All three compiled NodeGateway class files match the Node artifact; all five RelayServer class files match Node, Cloud and relay artifacts. Evidence: `/private/tmp/parlons-host-limits-{focused,family,core-tests,desk-tests}.log`.

Code-review verdict: approve for the reproduced capacity/reset/overflow/cleanup faults. **Sovereignty:** the gain is bounded local admission state without erasing active clients' budgets. Each operator independently runs the same replaceable gateway/relay; no coordinator, registry, shared blacklist, central fallback or mandatory service is introduced. The existing gateway remains optional and its configured rates/disable controls remain available. Signed ownership, message encryption, relay selection and classic interoperability are unchanged. Record-count bounds are not total heap-byte bounds or Sybil/fairness guarantees. A saturated table can refuse a legitimate new key. Relay reclamation waits for the existing maintenance loop after the minute expires, normally up to its next 30-second tick; a delayed maintenance loop delays recovery further. Gateway rescanning can wait up to one second after a previous scan. Gateway refill remains monotonic; relay wall-clock rollback can extend refusal. Short in-memory operations and bounded cleanup now serialize per table, which trades some contention for correct admission; no network or disk operation runs inside these rate-table locks. No throughput benchmark is claimed.

**Propagation:** Android 0.6.119/code 719, portal 0.2.66/code 110, Swing 1.5.98, Cloud 0.11.101, Node 0.2.104 and relay 0.4.99 were built. The standalone optional wake proxy and iOS app/extension require no source change and remain 0.1.5 and 0.1.31/build 29 respectively. No phone operation, funded transfer, installed desktop replacement or fleet deployment was used.

Code commit `e55b1fbf6c5839ad072eb468978cdfb53402d79a` is pushed and its exact [CI run 34523086312](https://github.com/eurobuddha/maxima/actions/runs/34523086312) passed. [Node 0.2.104](https://github.com/eurobuddha/maxima/releases/tag/node-v0.2.104) is published with SHA-256 `aa66cc38c78d68434faa4753a31251d5b2c66709bd67ba46e4e1b531d76ba71a`. Both unchanged desktop fetch scripts verified the release checksum, and both resource jars match the tested staged artifact byte-for-byte. minimaCore Desktop propagation commit: `29c5f11d646a48baabef8fee526ff04a517d7bd4` (0.16.78; separate pending working version 0.16.79 retained). minimaDesk propagation commit: `81bb89e51440b963c4ec07b81af03846895097b8` (0.7.62). Each changes only its two package manifests. Eight preserved minimaCore paths still match the fresh pre-propagation hashes. No desktop installer or running fleet service was updated.

The owner then moved the audit into closeout. This completes the in-flight repair; further review targets are deferred. Wake-proxy centralisation is a separate subsequent goal and remains unresolved. See [the final closeout report](AUDIT-CLOSEOUT-2026-09-10.md).

## Verification

- JVM full-family suite: **424 tests**, zero failures/errors/skips in the final run: core 253, server 70, Cloud 64, Node 10, classic maxjar 1, wake proxy 20, standalone desktop 6. Cloud and desktop each execute the shared six-case file-counter suite. The load-shedding continuation reran this entire family gate successfully. The earlier stale-relay pass's initial isolated timeout is recorded above.
- Core wire compatibility: **63/63 golden vectors** passed.
- JavaScript: **18 tests** passed: shared panel 8, wrapper sessions 5, HTTP bridges 5.
- Swift package suite: **94 tests** passed in one full release-configuration run. Includes concurrent sends, cancellation, deadline, closed-link retention, relay-reaping, RPC/remote lifecycle, callback-completion, five cache fixtures, eight mailbox challenge cases, three timestamp cases, 14 delivery/admission cases, three ACK queue cases and the extreme RPC timeout case. The old-code cache stress stall is recorded above.
- iOS app suite: **36 tests** pass in the simulator, including seven session lifecycle cases, seven relay-preference cases, seven background-completion cases, eight catch-up cases and six cached-refresh cases. Final application and notification extension compile as **0.1.31 build 29** for simulator and physical iPhone; both bundle plists match. Strict signature verification, installation and launch succeeded, and the device reports that version. The previous 0.1.20 startup logs confirm relay attachment, push registration and catch-up, with the delay described above.
- Android and portal release builds passed. Their unit-test tasks contain no tests; successful compilation is not physical-device verification.
- minimaDesk TypeScript checking and production Vite build passed; minimaCore changed JavaScript passed syntax checks.
- CI now runs the shared panel tests and the account/wallet/wake/Node JVM tests, and both desktop build workflows run their RPC/session regressions. `skipAndroid` now excludes the portal as well as the standalone Android app. The equivalent local JVM configuration passed, and the expanded [GitHub shared-engine/panel run](https://github.com/eurobuddha/maxima/actions/runs/34369943095) completed successfully for the panel-polish code commit. The RPC lifecycle continuation passed the full local family gate and its [GitHub CI run](https://github.com/eurobuddha/maxima/actions/runs/34372943896). The [payment retry CI run](https://github.com/eurobuddha/maxima/actions/runs/34374151839) also passed. The counter continuation passed its local tests/builds and [GitHub CI run](https://github.com/eurobuddha/maxima/actions/runs/34375374838). The worker-lifecycle continuation passed its full local gate and [GitHub CI run](https://github.com/eurobuddha/maxima/actions/runs/34380288309). The desktop matrix workflows were updated but no installer release was triggered.

No claim is made about real-device APNs delivery, power loss during filesystem fallback, full end-to-end calling/media/payment behavior, or the absence of all cryptographic/consensus defects.

## Further cohesion without centralisation

Two concrete consolidations are already in place: one durable file-counter implementation for the JVM account hosts, and one account panel for Cloud/Node/both Electron integrations. Their tests now gate the relevant build paths. Keep the Android preferences adapter and embedded full-node wallet adapter separate because their persistence contracts differ.

The next useful extension is a versioned **build-time compatibility manifest** recording the shared core commit, Node artifact hash, wire-fixture revision and host versions. Each release can validate its bundled artifact against that manifest using the existing checksum fetch scripts. It should never become a runtime registry or a publisher-controlled requirement for joining the network.

The independent-relay continuation now supplies an isolated anchor-outage fixture with built-in bootstrap disabled, signed replication, quorum checks and encrypted delivery. Expand the existing interoperability fixtures further with Java/Swift cross-language request, receipt and pairing cases, plus relay churn and conflicting-answer scenarios. These should check independent operation continuously while retaining replaceable infrastructure.

Other follow-up review areas are executor/connection shutdown under sustained churn, bounded per-peer work in serial lanes, live APNs HTTP/2/TLS integration and connection reuse, wake request-reader timeouts and gateway/relay admission throughput under load, concurrent remote reconnect/account switching, total ACK storage across connections, in-flight RPC transport ownership across expiry/closure, broader timeout arithmetic, total RPC work across retired peers, iPhone startup RPC latency, cross-process account/cursor coordination and cache durability, total attached relay population bounds, and cached-message reconciliation during reconnect. These are review targets, not asserted vulnerabilities: this pass did not establish a reproducible defect for each. Avoid speculative protocol changes or central fallback services in their name.
