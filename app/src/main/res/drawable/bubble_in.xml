<?xml version="1.0" encoding="utf-8"?>
<!-- Received, tail bubble (last of a run): neutral surface, the bottom-left
     corner flattened into a nub pointing at the other person's edge. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/ux_bubble_in" />
    <corners android:topLeftRadius="19dp" android:topRightRadius="19dp"
        android:bottomRightRadius="19dp" android:bottomLeftRadius="6dp" />
</shape>
