<?xml version="1.0" encoding="utf-8"?>
<!-- Sent, tail bubble (last of a run): WhatsApp-green, the bottom-right corner
     flattened into a nub pointing at the sender's own edge. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/ux_bubble_out" />
    <corners android:topLeftRadius="19dp" android:topRightRadius="19dp"
        android:bottomRightRadius="6dp" android:bottomLeftRadius="19dp" />
</shape>
